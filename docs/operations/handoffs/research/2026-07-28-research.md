# RESEARCH LAB HANDOFF — 2026-07-28

**Role:** Dreamboard Research Lab — research, competitive analysis, creator-workflow study, evidence synthesis. **No production code.**
**Product:** Dreamboard (primary, per Founder role brief) · WM Pro (ops bus host)
**Repo / branch:** ops bus `spaidsnipes/wealthymindsets-pro` @ `main` · subject repo `spaidsnipes/ABOVE_THE_HILL_DEVELOPMENTS_BUILT_APP_DREAM_BOARD`
**Start commit (bus):** `e1a8c94` · **End commit (bus):** `e1a8c94` + this docs-only change
**Tickets claimed:** none executable — see §5
**Tickets advanced:** DB-OPS-P1-01 (evidence corrected, next action reversed) · RISK-008 raised · DEC-009 requested

Evidence labels used: **VERIFIED** / **PARTIALLY VERIFIED** / **NOT VERIFIED** / **UNKNOWN** / **CONTRADICTED**.
Additional labels required by my role brief: **REPOSITORY** (observed in git) · **RUNTIME** (observed executing) · **DOCUMENTATION** (asserted in a doc) · **FOUNDER INTENT** (stated by Dave) · **INFERENCE** (my reasoning, not observation).

---

## 1. What I did

Ran the session-start checklist against the live bus for the first time (it existed this
session; four prior sessions found nothing). Read `ATH_COMMAND_CENTER.md`,
`ACTIVE_TASK_QUEUE.md`, `EMPLOYEE_STATUS.md`, and the newest handoffs
(`handoffs/sentinel/2026-07-28-sentinel.md`, `docs/HANDOFF_2026-07-28_FORGE.md`).

Found no eligible Dreamboard P0 to claim (§5). Redirected the session to the one thing a
research role can do without production code and without duplicating an active claim:
**auditing the evidence the bus is currently carrying about Dreamboard.** One record is
materially wrong, and acting on it as written would have caused a schema collision.

Also produced a competitive study earlier in this session (§6, carried forward with a
staleness caveat).

---

## 2. Commits created

**None in either repo.** This handoff plus the queue/status/risk edits are the only
changes, and they are docs-only on the bus. No production code was written or proposed for
direct application, per role boundary.

---

## 3. What I verified

All checks run 2026-07-28 in `/Users/dspaidnoosleep/dreamboard`.

### 3.1 The Dreamboard row in ATH_COMMAND_CENTER.md is stale — **VERIFIED / REPOSITORY**

The command center (§"Other products") records Dreamboard's last commit as `ba91915`
(2026-07-23). That is the **local branch tip**, not the product's state.

| Field | Command center says | Actual (`git fetch` this session) |
|---|---|---|
| Last commit | `ba91915`, 2026-07-23 | **`2049bdd`** on `origin/main` — *feat(growth): add private Growth Rings progress wall* |
| Local branch | `feature/project-memory-health` | same — **17 commits behind `origin/main`** |

`git log --oneline ba91915..origin/main` returns 17 commits, including nine Passport
commits (device-code sign-in, secure WOW World handoff, secure activity display), a public
front-door entry, archive intake guards, and `8e71195 feat: add inspectable project memory
and creative health`.

**Consequence:** the bus currently under-reports Dreamboard by 17 commits and five days of
shipped work. Any prioritization decision made from that row is made on bad data.

### 3.2 The 164 lines of "unpushed WIP" are a **superseded duplicate**, not lost work — **VERIFIED / REPOSITORY**

DB-OPS-P1-01 describes 164 lines of untracked Dreamboard WIP and gives next action
*"Commit and push, or record why it is parked."* **Committing and pushing it would be a
defect.** The same feature already shipped upstream in `8e71195`, under different filenames.

| Local untracked (this machine only) | Upstream on `origin/main` (`8e71195`) |
|---|---|
| `app/memory.tsx` — 97 lines | `app/memory-health.tsx` — 126 lines |
| `lib/creative-health.ts` — 44 lines | `lib/memory-health.ts` — 105 lines |
| `supabase/dreamboard-project-memory.sql` — 23 lines | `supabase/dreamboard-project-memory-health.sql` — 50 lines |
| *(no test)* | `tests/memory-health.test.mjs` — 19 lines |

Name-matching alone would be weak evidence, so I compared substance:

- **Identical database object.** Both migrations run
  `create table if not exists public.dreamboard_project_memory` and
  `create policy "Creators manage their own project memory"` — **same table, same policy
  name.** Two migration files defining the same object is the collision.
- **Upstream is a strict superset.** It adds a second table
  (`dreamboard_creative_health_preferences` + its own RLS policy), a five-value
  `MEMORY_SCOPES` and eleven-value `MEMORY_CATEGORIES` taxonomy, label maps,
  `validateMemoryDraft()`, and a unit test. The local draft has none of these.
- **The health function is the earlier draft of the same function.** Local
  `creativeHealth(input: HealthInputs): HealthSignal[]` ↔ upstream
  `deriveCreativeHealth(input: HealthInput): HealthObservation[]`.

**INFERENCE (not observation):** the local files are a working draft from the session that
produced `8e71195`, left behind after the polished version was committed under clearer
names. I did not confirm authorship — see §4.

### 3.3 The bus is now real and readable — **VERIFIED / REPOSITORY**

`docs/operations/` exists at `~/wealthymindsets-pro`, tracked, seven documents plus five
role handoff directories. Prior sessions verified its absence four times; that blocker is
genuinely cleared. `handoffs/research/` contained only `.gitkeep` — this is the first
Research entry.

---

## 4. What I did NOT verify

- **Authorship of the local Dreamboard WIP — UNKNOWN.** Untracked files carry no author.
  My superseded-duplicate conclusion rests on content, not provenance. If a human
  deliberately parked a *divergent* design there, my read is wrong. **Ask before deleting.**
- **Whether the local draft contains anything upstream lacks — NOT VERIFIED.** I compared
  exported symbols, table names, policy names, and line counts. I did **not** diff
  line-by-line. A small local idea could exist that never made it upstream. Required before
  any discard.
- **Dreamboard build/type/test at `origin/main` — NOT VERIFIED.** I ran no `npm run build`,
  no `tsc`, no `npm test` in the Dreamboard repo this session. The 17 upstream commits are
  **unverified by anyone** as far as the bus records — no Dreamboard entry exists in
  `VERIFICATION_QUEUE.md`.
- **Dreamboard runtime behaviour — UNKNOWN.** No dev server started, no browser driven,
  no screenshot. Every claim in §3 is repository evidence only. Nothing here is a RUNTIME
  claim.
- **Whether `2049bdd` is deployed — UNKNOWN.** Not checked; Dreamboard's deploy target is
  not recorded on the bus.
- **My own competitive study's current accuracy — PARTIALLY VERIFIED, see §6.**
- **WM-RESEARCH-P1-01 — nothing done.** Forge holds the lead claim and is ACTIVE. I did not
  open TradingView or tastytrade. Not blocked; deliberately not duplicated.

---

## 5. Blockers hit

### BLOCK-R1 — My role brief and the bus disagree on what I should work on

- **FOUNDER INTENT:** my brief says *"Support the Dreamboard P0 workflow first; keep
  speculative P1/P2 ideas out of release-critical implementation."*
- **DOCUMENTATION (bus):** `ATH_COMMAND_CENTER.md` names WM Pro the sole active product,
  with the 2026-07-31 chart/heatmap release objective, and states Dreamboard is explicitly
  **not** in this work block.
- **REPOSITORY:** the queue contains **no Dreamboard P0**. The only Dreamboard entry is
  DB-OPS-P1-01, a **P1** hygiene item. `EMPLOYEE_STATUS.md` lists Research Lab as
  *"Support WM-RESEARCH-P1-01"* — a **WM Pro P1**.

So the brief orders me to prioritize a P0 that does not exist, while the bus assigns me to
a P1 belonging to another product and already led by Forge. **CONTRADICTED — Founder must
resolve.** Raised as DEC-009 (§6). I did not guess: I neither invented a Dreamboard P0 nor
duplicated Forge's claim.

### BLOCK-R2 — Dreamboard has no verification lane

Sentinel owns verification, but the bus scopes Sentinel to WM Pro. 17 Dreamboard commits
are unverified with no owner and no queue entry. Not mine to fix — flagging it.

---

## 6. Risks and decisions raised

**RISK-008 (new, MEDIUM) — Superseded Dreamboard WIP is recorded as work to push.**
DB-OPS-P1-01 as written instructs an employee to push a migration that redefines a table
and policy already created upstream. Mitigation: record the finding (done), reverse the
ticket's next action (done), require a line-level diff before discard. See §3.2, §4.

**RISK-009 (new, MEDIUM) — The bus's cross-product rows are stale by construction.**
Dreamboard's row was wrong within hours because it was derived from a stale local clone
rather than `origin/main`. Any product tracked from a local checkout will drift the same
way. Mitigation: cross-product rows should record `origin/<branch>` after an explicit
fetch, and name the fetch timestamp. **INFERENCE** — proposal, not an approved standard;
Sentinel's call per the Atlas rule.

**DEC-009 requested (Founder) — Which product does the Research Lab serve?**
Options: (a) Dreamboard is genuinely primary → a Dreamboard P0 must be defined and the
command center's "WM Pro only" scope amended; (b) WM Pro is primary through 2026-07-31 →
my brief's "Dreamboard P0 first" is superseded until Friday and I support
WM-RESEARCH-P1-01 as documentation only; (c) split. **Recommendation: (b) until Friday.**
The dated release objective is concrete and shared; "Dreamboard P0" currently names
nothing. This is a Founder decision, not mine.

**Carried-forward deliverable — `docs/research/world-class-study.md` (Dreamboard repo,
UNCOMMITTED).** A ten-product study (Notion, Obsidian, Capacities, Milanote, Scrivener,
Cursor, Claude Projects, Apple Notes, Arc, Canva) → seven principles → 19 tickets
(RL-01…RL-19). Honest caveats:

- It is **P1/P2 speculative work by construction**, and per my brief it must stay out of
  release-critical implementation. **No RL ticket should enter a P0 lane.**
- It was written against the **stale local tree** (`ba91915`) before I discovered the
  17-commit gap. Its structural claims — notably *"no command palette exists"* and
  *"`app/page.tsx:40` is a flat 24-item rail"* — are **NOT VERIFIED against `origin/main`**
  and may already be partly false. Nine Passport commits landed in that window.
- It is **not committed**, deliberately: the only local branch is 17 commits stale, and
  committing research onto a stale branch is the same class of error as §3.2.
- **Acceptance implication:** re-verify against `origin/main` before any RL ticket is
  costed. Two — RL-05 (creator-placed node positions) and RL-18 (template scaffolds) —
  need an **ADR under ADR-0002** before they can be built at all, because both touch the
  no-fake-data rule. They are blocked on a Founder ruling, not on engineering.

---

## 7. Exact next action for whoever picks this up

**Founder (Dave) — resolve DEC-009.** One sentence: is the Research Lab on Dreamboard or on
WM Pro until 2026-07-31? Everything else I do is guesswork until this is answered. Also
confirm whether the local Dreamboard WIP may be discarded once §4's line-level diff is
clean.

**Sentinel —**
1. Correct the Dreamboard row in `ATH_COMMAND_CENTER.md` to `origin/main` `2049bdd` (§3.1).
   I have edited `ACTIVE_TASK_QUEUE.md` and `RISKS_AND_BLOCKERS.md` but did **not** edit the
   command center: it is your file and it also carries the WM Pro release objective.
2. Rule on RISK-009's proposed standard (fetch-then-record for cross-product rows).
3. Decide whether Dreamboard gets a verification lane (BLOCK-R2), or is formally
   out of scope until Friday — either is fine, silence is not.

**Forge — no action required from me and no claim contested.** WM-RESEARCH-P1-01 remains
yours. When you want documentation support, the outstanding qualitative axes are already
enumerated in the ticket; I will format and evidence-label, not re-run, your observations.

**Noah — nothing here touches WM-CHART-P0-01.** No RL ticket, no Dreamboard finding, and no
recommendation in this handoff should reach your ticket. Ignore this document entirely
until Friday ships.

### The exact next research question

> **Which of the 19 RL findings survive re-verification against Dreamboard `origin/main`
> `2049bdd`, and which were already fixed by the 17 unreviewed commits?**

Bounded: repository evidence only, no runtime claims, no production code, output is a
one-page delta appended to `world-class-study.md`. It is the correct next question because
it is the cheapest way to stop a stale study from feeding a real backlog — and because if
DEC-009 lands on (b), it is also the *only* Dreamboard work worth doing before Friday.

**Not** the next question: anything requiring a Dreamboard dev server or browser. RISK-001
still stands and I will not produce a RUNTIME claim I cannot evidence.
