# NOAH HANDOFF — WM-UX-P0-01: Delta bubble-count control migrated to Smart Money panel

**From:** Noah (Implementation) · **Date:** 2026-07-31 · **Spec:** Micah `2026-07-31-micah-wm-ux-p0-01-delta-panel-migration.md`
**Commit:** `0270590` · **Branch:** `main` (pushed, `7e13292..0270590`) · **Status:** SHIPPED — awaiting Sentinel visual verify

---

## What changed

Moved the Delta level-count selector (`5 / 7 / 10 / 15`, default `7★`) out of the Big Trades gear dropdown and into the Smart Money panel's **WM DELTA BUBBLES** section, directly under the title/badge — the control now sits with the bubbles it governs.

### Files diffed
| File | Change |
|---|---|
| `src/components/smart-money/SmartMoneyPanel.tsx` | +65 — new `deltaLevelCap` state + setter (reuses `wm_delta_levels` key + `wm-delta-levels` event), cross-tab sync listener, and the segmented count control in the WM DELTA BUBBLES header. |
| `src/components/chart/FootprintControls.tsx` | −36 — removed the Delta-levels grid block **and** its now-unused `deltaLevels` state/setter from the Big Trades gear. |
| `src/components/chart/MainChart.tsx` | comment fix — the SM panel is now the broadcaster; read-side listener at `1166–1175` unchanged. |

## Design decisions honored (per spec §3)
- **Segmented control, not a slider** — 4 discrete meaningful values; unambiguous `aria-pressed` selected state.
- **Same storage** — `wm_delta_levels` key + `wm-delta-levels` event untouched, so `MainChart` and any other listener keep working; on-chart bubbles re-cap within one tick.
- **Touch/keyboard** — each segment `min-h-[44px]`, `text-[12px]`, `aria-pressed`, `role="group"`, Arrow Left/Right roving + focus-visible ring; Enter/Space commit via native button.
- **`7★` default marker** and honest helper wording ("max ranked price levels per bar") carried over.

## Acceptance mapping (spec §6)
| # | Criterion | Status |
|---|---|---|
| 1 | Control in SM panel; gone from Big Trades gear | ✅ code + grep |
| 2 | `wm_delta_levels` key + `wm-delta-levels` event unchanged; default 7 | ✅ |
| 3 | Change updates badge + list + on-chart bubbles, no layout shift | ✅ code (fixed-height control) — **Sentinel to confirm live** |
| 4 | ≥44×44 hit area; `aria-pressed`; keyboard-operable | ✅ code |
| 5 | Number font ≥12px | ✅ `text-[12px]` |
| 6 | Single source of truth | ✅ grep: only SM panel writes `wm_delta_levels`; MainChart read-only |
| 7 | Screenshots @ 360/390/834/desktop | ⛔ **BLOCKED for me** — see below |

## Verification done
- `node node_modules/typescript/bin/tsc --noEmit` → **clean (exit 0)**.
- `node node_modules/.bin/vitest run smart-money footprint delta chart` → **16/16 pass**.
- Grep single-source-of-truth: only `SmartMoneyPanel.tsx` writes `wm_delta_levels`; `FootprintControls.tsx` retains a pointer comment only; `MainChart.tsx` is read-only listener.

## ⚠️ Visual verification handed to Sentinel
I could **not** self-capture the required screenshots: both the in-app preview browser and the connected Chrome redirect `/charts → /login`, and I will not enter the Founder's credentials (per standing security rule + WM identity note). **Sentinel must capture acceptance #7 (360×800, 390×844, 834×1194, desktop) against an authenticated session**, showing the control present in the SM panel and absent from the Big Trades gear, and confirm #3 (live re-cap, no layout shift). Mobile-width capture remains RISK-001/display-clamp constrained per Micah's baseline note.

**Next in my queue:** WM-VP-P0-01 (Session VP recurrence fix). Separately, Sentinel's `1005-sentinel-to-noah-revert-tastytrade-order-lifecycle.md` + the DEC-005 flag on `853e699` need ACK/defend/revert — I will address before touching WM-BROKER-P0-01.
