# WM-WYCK-P0-02 — Deployment Provenance and Evidence Handoff

**Date:** 2026-07-28  
**Product:** WM Pro  
**Role:** Noah  
**Ticket:** `WM-WYCK-P0-02`  
**Status:** READY FOR SENTINEL CLOSURE  
**Production code changed:** No  
**Brokerage actions:** None; runtime work was read-only.

## Objective and acceptance criteria

Close deployment provenance for the truthful Wyckoff correction by recording provider evidence for the production deployment and binding runtime, typecheck, test, and production-build evidence to exact source SHA `e1a8c949b224eb45aaa541846d36e70560782b45`.

Acceptance requires:

1. Provider deployment ID, production URL, UTC deployment time, branch, and exact SHA.
2. Two-symbol runtime proof showing the truthful unavailable state and absence of the fabricated schematic markers.
3. TypeScript, focused regression, full test suite, and 69-page build evidence from the exact SHA.
4. No production-code changes and no unrelated dirty-file changes.

## Provider deployment provenance

Direct Vercel dashboard evidence:

| Field | Provider-reported value |
|---|---|
| Project | `wealthymindsets-pro` |
| Deployment ID | `dpl_6pWrxC8DDXor5xkgV5Aq9RSDemMp` |
| Dashboard route key | `6pWrxC8DDXor5xkgV5Aq9RSDemMp` |
| Status | Ready, Latest |
| Environment | Production, Current |
| Canonical production URL | `https://wealthymindsets-pro.vercel.app` |
| Immutable deployment URL | `https://wealthymindsets-fwy92bpqw-spaidsnipes-projects.vercel.app` |
| Git branch | `main` |
| Git SHA | `e1a8c949b224eb45aaa541846d36e70560782b45` |
| Commit subject | `fix(charts): remove fabricated Wyckoff current phase` |
| Created | 2026-07-28 12:28:19 PM CDT |
| Created UTC | **2026-07-28 17:28:19 UTC** |
| Provider build duration | 1m 3s |

Provider screenshot: [`vercel-provenance.png`](../../evidence/wm-wyck-p0-02/vercel-provenance.png)

## Runtime acceptance evidence

Canonical runtime checked at:

`https://wealthymindsets-pro.vercel.app/charts`

The authenticated chart was hard-refreshed after deployment before evidence capture. The Smart Money panel was opened read-only. No order-entry control was used.

For both `AAPL` and `TSLA`, DOM-level runtime checks returned:

- `N/A — phase model not implemented`: present.
- `No phase is inferred from a single price/tape snapshot`: present.
- `Spring / Shakeout`: absent.
- standalone `CURRENT`: absent.

Evidence:

- [`aapl-runtime.png`](../../evidence/wm-wyck-p0-02/aapl-runtime.png)
- [`tsla-runtime.png`](../../evidence/wm-wyck-p0-02/tsla-runtime.png)

The pre-refresh browser tab briefly retained the prior single-page-application state and still showed the old schematic below the new truthful block. A hard refresh loaded the current production bundle; both post-refresh symbol checks passed. This is recorded so future runtime verification refreshes the page after a deployment instead of trusting an already-open tab.

## Exact-SHA automated verification

Verification ran from a clean `git archive` of:

`e1a8c949b224eb45aaa541846d36e70560782b45`

The canonical dirty checkout was not used for compilation and was not modified.

| Check | Result |
|---|---|
| `./node_modules/.bin/tsc --noEmit` | PASS, zero errors |
| Focused `SmartMoneyPanel.truthfulness.test.ts` | PASS, 1/1 |
| `npm test` | PASS, 12/12 across 2 files |
| `npm run build` | PASS, 69/69 static pages |

An initial environment-free build compiled and typechecked but stopped during page-data collection because `/api/upload-track` constructs a Supabase client at module load and requires `NEXT_PUBLIC_SUPABASE_URL` plus `SUPABASE_SERVICE_ROLE_KEY`. The successful evidence build used non-secret, syntactically valid placeholder values only. No production credentials were read, copied, printed, or transmitted. This is a reproducibility limitation, not a Wyckoff regression.

The previously repeated 26/26 count belongs to Dreamboard or requires a different exact command. WM Pro's current complete `npm test` result is 12/12.

## Repository and preservation proof

Before and after evidence collection, the canonical checkout remained:

- `main` = `origin/main` = `e1a8c949b224eb45aaa541846d36e70560782b45`.
- Existing modified files preserved and not staged:
  - `docs/operations/ACTIVE_TASK_QUEUE.md`
  - `docs/operations/ATH_COMMAND_CENTER.md`
  - `docs/operations/RISKS_AND_BLOCKERS.md`
  - `src/app/lounge/page.tsx`
  - `tsconfig.tsbuildinfo`

No production source file changed in this ticket.

## Deployment and release result

`WM-WYCK-P0-01` is now bound to a provider-confirmed production deployment of exact SHA `e1a8c94`, and the post-refresh two-symbol runtime acceptance passes. No remediation ticket is required for the Wyckoff truthfulness correction.

## Known limitations

- The screenshot viewport shows the active Smart Money panel and selected symbol; the exact text assertions above were captured from the live DOM because the Wyckoff subsection sits below the visible panel fold.
- The build requires syntactically valid Supabase configuration at page-collection time even for unrelated code. A future bounded build-reproducibility ticket may defer client construction or fail with a clearer configuration message.
- This evidence does not verify provider/timeframe support and makes no claim about Forge-owned `WM-CHART-P0-01`.

## Exact next ticket and owner

There is no currently approved, dependency-clear, non-overlapping Noah implementation ticket in the WM Pro queue:

- `WM-CHART-P0-01` remains Forge-owned.
- `WM-CHART-P0-02`, `WM-HEAT-P0-01`, `WM-STATE-P0-01`, and `WM-TEST-P0-01` are blocked by Forge's canonical-timeframe work.
- Security/database work remains blocked by Founder approval and backup requirements.

**Exact next action:** Sentinel verifies this evidence packet and closes `WM-WYCK-P0-02`. Noah returns to Dreamboard P0 and remains available for a newly approved bounded WM Pro ticket. Noah must not claim or duplicate `WM-CHART-P0-01`.
