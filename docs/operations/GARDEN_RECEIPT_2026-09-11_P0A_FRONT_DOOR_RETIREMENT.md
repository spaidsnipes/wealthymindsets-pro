<!-- ath-retirement-evidence -->
# GARDEN RECEIPT — P0-A REPO FRONT-DOOR RETIREMENT + SEARCH

**This is a dated receipt.** It is evidence of what was true on 2026-09-11. It may teach.
It may not command.

> This document NAMES the retired Vercel host on purpose — it exists to record that
> the host was retired, and a retirement record that may not name the retired thing
> cannot be written. The `<!-- ath-retirement-evidence -->` marker above declares
> that intent to the GHOST_HOST guard in `src/lib/ops/repoFrontDoorAuthority.test.ts`.
> It is a declaration of purpose, not an exemption from truth: the host named below
> is retired and unreachable, and nothing here directs a reader to visit it.

| Field | Value |
|---|---|
| `GATE_ID` | G1 Soil/Source · G2 Root/Owner · G12 Compost/Retirement |
| `PRODUCT` | WM Pro |
| `CHECK_ID` | `P0-A-REPO-FRONT-DOOR` |
| `CANONICAL_OWNER` | Drive — `ATH — CURRENT COMMAND CENTER — 2026-09-11`; `ATH — FULL GARDEN PASS — 2026-09-11` |
| `STATE` | **PASS** |
| `MATURITY` | MACHINE-ENFORCED (was WRITTEN + PROCESS-ENFORCED) |
| `OBSERVED_AT` | 2026-09-11 |
| `REVALIDATE_AFTER` | Continuous — `repoFrontDoorAuthority.test.ts` runs on every suite execution |
| `SOURCE` | Working tree at `c8bd388` → repaired at `0b8acef` |
| `FAILURE_CLASS` | `DUPLICATE_TRUTH` + `GHOST_HOST` + `RETIREMENT_DEBT` |

## Observed failure

`README.md` routed "ATH employees start here" → `docs/operations/ATH_COMMAND_CENTER.md`,
which declared itself "the single entry point for every ATH employee", was stamped
`2026-07-28`, drove a release objective of `2026-07-31`, and named the deployment target
`wealthymindsets-pro.vercel.app`. The same README offered `http://localhost:3000` under
"Access the App (RIGHT NOW)".

A fresh worker starting at the repository front door reached July, a retired host, and a
development URL presented as the app — without ever being told they had left present
authority.

## Search performed (the "every alternate current-looking entry" requirement)

All **82** `docs/operations/*.md` files scanned for three independent signals: Vercel
references, `localhost:3000` references, and door language (`start here` / `entry point` /
`read before` / `source of truth`).

- **Dated filenames** (≈60) are evidence by construction. README now states this rule
  explicitly, so they need no individual header.
- **Undated or currently-named files** were read individually. Ten required demotion:

| File | Why it read as a current door |
|---|---|
| `ATH_COMMAND_CENTER.md` | "single entry point for every ATH employee" |
| `ACTIVE_TASK_QUEUE.md` | live claim protocol + ticket statuses |
| `BUILD_STATUS.md` | build/suite/deploy figures as a standing claim |
| `RISKS_AND_BLOCKERS.md` | `OPEN` risk markers readable as present blockers |
| `CURRENT_WM_PRO_BRIEF.md` | **"this brief wins for OPERATIONAL truth"** — an explicit competing authority claim |
| `DAILY_OPERATIONS_REPORT.md` | 2026-07-31 GO-LIVE GATE |
| `EMPLOYEE_STATUS.md` | live per-employee assignment rows |
| `VERIFICATION_QUEUE.md` | live verification assignments |
| `TEAM_CHARTERS.md` | "default-when-idle work" routing, cited by the Command Center |
| `CURRENT_STATE_2026-08-23.md` | `CURRENT_STATE` name + localhost + Vercel |

**Deliberately NOT demoted**, with reasons:

- `CLOUDFLARE_DEPLOY_GUIDE.md` — describes the **current** host; demoting it would be a
  second error in the opposite direction.
- `WOW_RESPONSIVE_STANDARD.md`, `CONFIDENCE_ENGINE.md`, `DECISIONS.md`,
  `ATH_WOW_SUPER_BUILDER_CONTRACT_v1.md`, `ATH_WOW_TEAM_PROMPT_v1.md` — standards,
  principles and append-only ledgers. These TEACH; they do not route current work, and
  the mobile/visual-confirmation standard in particular remains binding.
- Templates (`*_TEMPLATE.md`) — shapes, not instructions.

**Nothing was deleted.** Lineage is preserved; only the right to command was withdrawn.

## Evidence

- Production verified before it was written into README: `https://wealthymindsetspro.com/login`
  → `HTTP 200`; `www` → redirects to apex. 2026-09-11.
- `tsc --noEmit --skipLibCheck` → exit 0.
- `vitest run` → **496 files / 5570 tests passed** (from 495 / 5563; the delta is the new
  Sentinel).
- Commit `0b8acef`, pushed to `origin/main`.

## Revive proof (REVIVE LAW)

| Revive | Injected defect | Result |
|---|---|---|
| J | Stripped the lineage marker from `ATH_COMMAND_CENTER.md` | `every operations doc README mentions is already marked historical` **FAILED by name** |
| K | Restored `wealthymindsets-pro.vercel.app` as production in README | `README does not present the RETIRED host as current production` **FAILED by name** |
| L | Reinstated the `(RIGHT NOW)` heading above the dev URL | `README does not present a development URL as production` **FAILED by name** |

All three restored byte-identical, confirmed by `diff`.

**RECORDED GAP — `tsc` exited 0 under all three revives.** Markdown is neither
typechecked nor imported, and the repo reports ZERO GitHub rulesets. The type system is
structurally blind to authority drift in prose. `repoFrontDoorAuthority.test.ts` is the
only machine in that path, which is precisely why it derives over the real tree instead
of listing what this session happened to demote.

## Human / capital consequence

A worker (human or AI) starting at `README.md` no longer loses a shift to a finished July
milestone on a retired host, and no longer reports a green `localhost:3000` as evidence
about what the Founder's browser receives.

## Affected capability

Repository entry system. No runtime surface changed. **No trading, market-data or capital
behaviour was touched by this atom.**

## CURRENT / NEXT / PLANT

- **CURRENT** — P0-A is `PASS` and machine-enforced.
- **NEXT** — return immediately to the WM hot path: **P0-C**, real option producer →
  canonical option truth → attached Option Expression → normal browser scene.
- **PLANT** — the `<!-- BEGIN:ath-historical-lineage -->` marker is now a reusable
  vocabulary. Any future retirement stamps the same marker and inherits the same machine.
