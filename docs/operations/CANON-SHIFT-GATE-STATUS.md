# CANON — SHIFT GATE STATUS

**Any agent working the ATHOS WM Pro shift reads the gate list HERE, not from the shift prompt.**

## Why this file exists

The standing shift prompt carries a list of "remaining gates". That list is
retyped from shift to shift, by hand, from memory. On 2026-09-18 two of its
entries were measured and found already closed:

- **Delta Bubbles level ownership** — closed by `src/lib/deltaBubbleLevels.ts`
  plus an adoption sentinel, 18 tests green.
- **Live VP render geometry proof** — closed by `scripts/prove-vp-pixels.mjs`,
  which paints through the shipped geometry owners on a real browser canvas and
  reads the bytes back, and which can be made to fail five different ways.

Nothing was wrong with the code. **The list had drifted from it.** That is the
identical failure `CANON-VIEW-BUILD-ORDER-2026-09-17.md` records for the view
build order, where an operator spent three atoms re-opening shipped work.

A work-list retyped each shift is a CONVENTION. **AN OWNER BEATS A CONVENTION.**
The wrong answer a stale gate list produces is not a broken pixel — it is a
whole shift spent re-proving something already true, and it leaves no trace,
because re-opened work looks exactly like work.

The table below is gated by `src/lib/ops/shiftGateStatus.sentinel.test.ts`.
Prose in this file is commentary. **The table is the claim.**

## What a STATE means here

| STATE | Meaning |
|---|---|
| `CLOSED` | An instrument exists, was run, and passed. The EVIDENCE column names what to run to see it again. |
| `OPEN` | Real remaining work. Nobody is blocked; it has not been done. |
| `BLOCKED` | Cannot proceed for a reason outside this repo. The blocker is NAMED. |

`CLOSED` is a claim about an INSTRUMENT, not about beauty. A gate can be closed
and the surface still ugly; that is a different gate and should be its own row.

<!-- GATE-STATUS:BEGIN -->
| GATE | STATE | OWNER | MEASURED | EVIDENCE |
|---|---|---|---|---|
| Delta Bubbles level ownership | CLOSED | `src/lib/deltaBubbleLevels.ts` `src/lib/deltaBubbleLevels.adoption.sentinel.test.ts` | 2026-09-18 | Run `./node_modules/.bin/vitest run src/lib/deltaBubbleLevels.adoption.sentinel.test.ts` — EXIT 0. The extraction fixed two real defects the sentinel now pins: a bubble printing a bucket CENTRE as the price flow happened at, and a rounded price used as a bucket identity, which silently merged buckets and dropped aggressor volume. |
| Live VP render geometry proof | CLOSED | `scripts/prove-vp-pixels.mjs` `src/lib/vpDrawGeometry.ts` | 2026-09-18 | Run `npm run prove:vp-pixels` — EXIT 0, 5 pixel laws hold against real painted bytes read via `getImageData()`. The instrument is falsifiable: `WM_VP_REVIVE=ROW_HEIGHT\|BAR_CURVE\|SPLIT_ROUND\|FITS_LIE\|AXIS_BLEED` each exit 1 with 1/22/1/2/3 offences respectively. **SCOPE:** this proves the GEOMETRY OWNERS paint honestly. It does NOT prove MainChart's composed scene — colours, labels, POC/VAH/VAL lines over live price — which stays HUMAN_PROOF_REQUIRED and is its own row when someone opens it. |
| Decision Memory sealing has zero production callers | OPEN | `src/lib/traderMemory/decisionMemory.ts` `src/lib/decisionMemory.ts` `src/lib/decisionMemoryReachability.test.ts` | 2026-09-18 | Re-measured, still true, and NOT stale. `grep -rn "sealDecision\b" src/` finds no non-test caller outside the module and its store; `sealDecisionMemory` has none at all. **ARCHITECTURAL — SURFACE IT, DO NOT RUSH-WIRE.** The write path exists and is tested; what is missing is a decision ingress, and inventing one to make a caller appear would mint decisions no trader made. RE-VERIFIED 2026-09-19 by strict grep, still write-orphaned: the sole `sealDecision(` call site outside the module/store/tests is a DecisionReceiptPanel.test.tsx fixture helper; `decisionMemoryReachability.test.ts` continues to pin the READ-wired/WRITE-orphaned shape as a named blocker. NEW FACT FROM THE M3 QUARANTINE: the capability's richest read surface, DecisionReceiptPanel, is a DECK-ONLY organ of the now-LEGACY /command-deck (inventory suggests /journal as its migration target), and the other production readers (/profile, useMarketCanvasVM) already union with real Journal decisions. So the ingress question and the organ-migration question have COLLAPSED INTO ONE: whoever moves DecisionReceiptPanel out of the quarantined room decides where sealing is born, because wiring a writer whose primary reader lives in a legacy room would grow the quarantined surface instead of shrinking it. Decide ingress AS PART OF that migration atom — not before, not separately. |
| executionConnectivity orphaned | OPEN | `src/lib/authority/executionConnectivity.ts` `src/lib/authority/executionReadinessDisclosure.test.ts` | 2026-09-18 | `grep -rn "from .*executionConnectivity" src/` returns no non-test importer. **NOT A LIVE DEFECT** — `/readiness` discloses the absence honestly rather than claiming connectivity it does not have, and the disclosure test passes. This row exists so nobody re-derives that conclusion; deleting the module is a separate decision from wiring it. |
| Paper execution state machine realism | OPEN | `src/lib/paperTrade.ts` `src/lib/paperTradeOutcome.ts` `docs/operations/PAPER-EXECUTION-REALISM-STANDARD.md` | 2026-09-19 | THE STANDARD NOW EXISTS, 2026-09-19, at `docs/operations/PAPER-EXECUTION-REALISM-STANDARD.md`, and ZERO fills were changed to write it — which is exactly what this row demanded first. The definition it fixes: realism is fidelity to what was observed, not similarity to what a broker would have done; a fill is realistic when every number on the Trade was produced by a feed we received and every unsupported assumption is disclosed ON the record. It documents the floor the code already holds (observed-price fills in `selectOrderFill`, disclosed `queueBasis`, scratch-vs-absence in `paperTradeOutcome.ts`, `fillPriceAgeMs` staleness, market-orders-only in `selectCloseOrderPlan`) and a four-rung fidelity ladder — spread requires a real bid/ask, touch requires real tape, size requires L2 — each rung gated on the named observed input existing, with the forbidden fabrications (fixed-bps spread, probabilistic touch fills, random partials, latency jitter) named so they cannot arrive dressed as realism. WHAT KEEPS THIS ROW OPEN: rung 1 (fill against the observed spread side) is the first real fills change, and it is legal only when the /paper lane carries a genuine bid/ask observation at fill time. MEASURED 2026-09-19: it does not. `/api/yahoo?type=quote` builds its entire payload from the v8 chart endpoint's `meta` + intraday candles — `price/open/high/low/prevClose/volume` only; no `bid`, `ask`, `bidSize`, or `askSize` field exists in the route, its tests, or the upstream response it parses. RUNG 1 IS UNFUNDABLE ON THE CURRENT LANE, so the ladder correctly holds the fill path at rung 0 and any spread-side change offered before a real bid/ask feed lands is a fabrication per the standard's own review checklist (question 1: no new observed input). This row stays OPEN as the ladder's custodian — the next legal move is a data-lane change (a quote source that carries top-of-book), not a fills change. |
| Gate 4 responsive device proof | BLOCKED | `scripts/audit-phone-parity.mjs` | 2026-09-19 | **THE STATED BLOCKER WAS STALE AND IS WITHDRAWN. THE GATE STAYS BLOCKED FOR A DIFFERENT, SMALLER REASON.** This row read "programmatic window resize does not take effect — `outerWidth` stays pinned". Re-measured 2026-09-19 against live `https://wealthymindsetspro.com/login` with Playwright driving the INSTALLED Chrome (`channel:"chrome"`, no browser download): asked 1440x900 → `clientWidth:1440 outerWidth:1440 dpr:2 mqPhone:false`; asked 390x844 → `clientWidth:390 outerWidth:390 dpr:3 mqPhone:true`. `outerWidth` tracks exactly and the 640px media query flips. Resize works. The pinning is a property of the BROWSER-EXTENSION channel only — `audit-phone-parity.mjs:363` already said so in a comment that never reached this row, so the gate carried a channel limitation as if it were an environment law for a day. **BLOCKED BY:** reaching an authenticated PRODUCTION session from a scripted channel without a human typing a password. Not resize. This gate lifts the day that exists, and the Sentinel that caught this row is the reason it now says so — the withdrawal paragraph below explained the blocker at length while deleting the phrase the house scans for, which is how a blocker stops being noticeable. Detail: every route but `/login` is client-side guarded, and prod `/charts` redirects an unauthenticated Playwright context to `/login` (measured: `landed:/login`). Closing this gate needs a way to reach an authenticated prod session from a scripted channel without a human typing a password — it does NOT need a new browser. A narrower blocker, named. |
| C-101 — `/charts` 70% FLOOR AREA | OPEN | `scripts/prove-charts-floor.mjs` `src/components/experience/DecisionSpineBand.tsx` `src/components/chart/ChartsDashboard.tsx` | 2026-09-19 | **THIS GATE NOW HAS A RUNNABLE OWNER: `npm run prove:charts-floor`.** It exits **1** today — measured, and missing — exits **0** the day the floor meets spec, and exits **2** when it could NOT measure, because a refusal is not a failure and must not be scored as one. The verdict is taken against `pctRoom`, the MORE GENEROUS of the two denominators it prints, so a miss cannot be argued away as a framing dispute. It REFUSES rather than substituting the largest visible canvas when lightweight-charts has not mounted; a floor-area number computed from a heat lens or a sparkline would be a true measurement of the wrong element. Attribution walks the whole ancestor chain from `main[os-room]` down to the candle and reports every level: two earlier attribution designs were each true numbers about the wrong box — the first stopped at a 28px row on phone because the candle had already collapsed, and the second reported NOTHING because the room's single child contains the candle, which beside a 4.3% market is the most misleading output of the three. **MEASURED, AND IT MISSES — BADLY ON THE PHONE.** The IFC hard-hat set published 2026-09-19 carries a numeric target C-101 states as `charts 70% FLOOR AREA`. Measured with Playwright + installed Chrome, candle field = the mounted `.tv-lightweight-charts` rect via `getBoundingClientRect()`, settled >=9s and stable across re-reads: **1440x900 → 1087x660 = 55.3% of viewport / 62.5% of `main[data-testid=os-room]`. 390x844 → 354x30 = 3.2% of viewport / 4.3% of the room.** The phone gives the market THIRTY PIXELS. The 632px room is spent on `div.wm-chart-room-header` 125px + the timeframe/indicator toolbar ~153px + the decision spine 268px, and the phone also SCROLLS (`scrollHeight` 961 vs 844) where the desk does not, so "SAME ORGANISM" is already false at the scroll model. **THE COUNTERFACTUAL WAS MEASURED, NOT GUESSED:** hiding `section.wm-decision-spine` in the live DOM and letting lightweight-charts re-fit yields **71.4% vp / 80.7% room at 1440x900** and 19.5% vp / 32% room at 390x844. Desktop therefore MEETS the spec by relocating exactly one component — the spine is a 317x724 floor-taking right rail (22.6% of the chart container) and wants to be an overlay or drawer over the candles. Phone does not; it needs the room header and toolbar rows off the floor as well, and is a separate atom. **PROVENANCE, STATED PLAINLY:** these are LOCAL `next dev` numbers at tree `fdea9a1c`, not production. Prod `/charts` bounces an unauthenticated scripted context to `/login` (see Gate 4), so the measuring agent ran the repo locally and stubbed the `/api/auth/me` RESPONSE inside its own browser context to get past the client route guard. No password was typed, no JWT was minted, no prod account was touched, and nothing under `src/` was modified. The `ChartsDashboard.tsx` diff in that tree was read and is honesty-plaque logic with no layout change, so the geometry should match prod — but it is MEASURED LOCALLY and this row does not claim otherwise. Artifacts: `/tmp/wm-floor/` (`charts-1440.png`, `charts-390.png`, `*-nospine.png`). **THE PHONE WAS FIXED ON 2026-09-19 (`d0423ec4`) AND IT STILL MISSES — 45.1% IS THE CEILING, NOT A WAYPOINT.** Root cause of the thirty pixels was not the spine and not the chrome: the chart pane carried an inline `minHeight: 0` and was the ONLY flexible child in a column of inflexible rows, so it absorbed the entire shortfall. `.wm-chart-market-pane[data-market-primary="true"] { min-height: min(60svh, 460px) !important }` at phone width took the candle from **354x30 (4.3% of room) to 354x315 (33.9% vp / 45.1% room)** — 10.5x — with NOTHING hidden, because the phone already scrolls (961 vs 844) so the chrome and spine keep their pixels and move down. **THE CEILING WAS TESTED, NOT DERIVED.** Rather than reason about it on paper I raised the same rule to `min(95svh, 780px)` and re-ran `npm run prove:charts-floor`: the phone result was **byte-identical — 354x315, 45.1%**. The pane min-height is no longer the binding constraint; the chrome above it is. The experiment was reverted exactly (`git diff --stat` empty). **THE ARITHMETIC, SO NOBODY RE-DERIVES IT:** phone chrome above the market is **278px** of an ~697px room — room header 125px (orientation strip 80 = context 18 + actions 44, wrapped; plus tabs 44) and toolbar 153px (timeframes 44 as eight 44x44 buttons + a ~28px wrap row + pinned row 44). 70% of the room leaves a budget of **209px** for chrome. 278 > 209, so **C-101's 70% is arithmetically unreachable on phone without deleting chrome rows** — and every row in that 278px is 44px tall precisely because of the standing 44px tap-target commitment. **THIS IS A BLUEPRINT-vs-ACCESSIBILITY COLLISION AND IT IS FOUNDER SCOPE.** I did not compress the chrome unilaterally: quietly shaving tap targets to make a gate go green would trade a measured a11y commitment for a number, and the trade would be invisible in the diff. Desktop is the same shape of decision one level up — the spine must leave the floor (narrowing it to 240px only reaches ~66.9%), and S-501 names the plaque a persistent secondary chunk. Both halves of C-101 now await a ruling, not an implementation. |
| /journal detail canvas | BLOCKED | `src/app/journal/page.tsx` | 2026-09-18 | BLOCKED BY: zero journal entries exist to open a detail view on. Any proof would have to be built against a fabricated entry, and a canvas verified against invented data proves only that the fabricator agrees with itself. Unblocks the moment a real entry is logged. |
| M1 — live-market destination mall | CLOSED | `src/components/os/WMOperatingSystem.tsx` `src/components/experience/WMExperienceShell.tsx` `src/components/layout/ShellAccessParity.test.tsx` | 2026-09-18 | CLOSED BY RE-HOMING, NOT BY DELETION. The inherited claim was half-stale: the rail already defaulted shut on the instrument view (`WMExperienceShell.tsx:484`) and the mode bar already collapsed there (`:496`). The half that was real was the PHONE — at 390 the same room still carried a pinned five-door strip across the bottom of the market plus a 66px reservation, so /charts was a workspace on the desk and a consumer app with a tab bar on the phone. New `phoneDestinations` prop: on the instrument view the strip is not drawn and the rail itself becomes the phone's navigation, pinned, reached through the SAME masthead toggle the desk uses and carrying all 21 destinations where the strip carried 5 — a gain of sixteen doors, with an explicit 44px Close so an opened sheet is not a trap. Proof: `./node_modules/.bin/vitest run src/components/layout/ShellAccessParity.test.tsx` EXIT 0, 20 passed. Mutation receipts: forcing `phoneDestinations="bar"` went red on "draws NO pinned destination strip over the market"; removing the Close control went red on "gives the opened sheet a way back out". Both restored EXIT 0. LIVE-PROVEN on https://wealthymindsetspro.com/charts after `ba0f413e` deployed — probed in the Founder's own authenticated Chrome, NOT a local build: `phoneNav:false` (the strip element is gone from the document entirely), `sheetRule:true` (the phone sheet CSS ships), `provReservation:false` (the 66px reservation is no longer emitted). Opening the masthead toggle live returned `railOpen:true, doorsInRail:21, railClosePresent:true, railCloseLabel:"Close rooms"` — the sixteen-door gain is real in production, not only in a test. The Founder's rail was then closed again USING the new Close control, which both restored his state and proved the escape works: `railOpen:false, expanded:"false"`. WHAT IS STILL NOT PROVEN, stated plainly: the 390px VISUAL. The strip's absence is a render branch and therefore width-independent, so that half is proven; the pinned-sheet geometry under a real 390 viewport is not, because programmatic resize does not take in this environment — see the Gate 4 BLOCKED row. |
| M2 — Workspace/Tools FULL stage | CLOSED | `src/components/experience/RoomEquipmentLayer.tsx` `src/lib/workspace/useEquipmentJourney.ts` `src/components/experience/RoomEquipmentLayer.test.tsx` | 2026-09-18 | RE-MEASURED AND LARGELY REFUTED, then the one real gap was closed. All three inherited observations are literally true and none is the defect the claim assumed. FULL takes the chart away ONLY at full, deliberately, and it is the single stage carrying RETURN; `useEquipmentJourney` restores the ROOM's exact scrollTop on the way out and its own comments record that reading window.scrollY instead was tried and measured wrong. Fixed `inset:0` is not the bug, it IS the fix — a fixed overlay is what keeps the chart MOUNTED underneath, so symbol, timeframe, MarketObjects and chart state survive by construction rather than by restoration. The URL carries a REFLECTION, not a source of truth, and `full` is explicitly not cold-openable so a shared link cannot drop a stranger into a full-screen reading with no chart behind it. WHAT WAS GENUINELY UNGUARDED: thirty-plus assertions next door check where the grammar is wired and what it refuses, and not one checked that the shell stays OUT of the room's layout flow — so a future simplification of FULL into an in-flow section would reflow the chart out of the room and the whole suite would stay green. Three tests added: every open depth is out of flow, only FULL claims `inset:0`, and the decision identity is published unchanged at every depth. Proof: `./node_modules/.bin/vitest run src/components/experience/RoomEquipmentLayer.test.tsx` EXIT 0, 12 passed. Mutation receipts: turning FULL into `position:relative` went red on the out-of-flow test AND the inset test; deleting the `data-decision-id` attribute went red on the identity test; both restored EXIT 0. |
| M3 — Command Deck route gravity | OPEN | `src/app/command-deck/page.tsx` `src/lib/routing/wmDestinations.ts` | 2026-09-19 | RE-MEASURED 2026-09-19, and the deck's remaining AUTHORITY was smaller than this row assumed. Authority surface 1, the LANDING, was already gone: `founderLanding.ts` has pointed every no-destination arrival (root redirect, AuthContext signed-in-on-public, auth/confirm, manifest start_url) at /charts since the 2026-09-17 cut — nothing about the deck had to move for Shot 1's routing half. Authority surface 2, the PHONE SLOT, was cut THIS SHIFT: `/command-deck` held one of five permanent doors in `PHONE_SLOT_HREFS`, which on the smallest screen presented the deck as a peer top-level home beside Charts. It is removed; the strip carries four doors and `ShellAccessParity.test.tsx` pins the count EXACTLY at four with an explicit `not.toContain("/command-deck")`, so regrowing the slot needs a diff. THE PRIOR ROW'S STANCE IS OVERRULED AND THE OVERRULING IS RECORDED: this row previously said "parity is the precondition for retirement, not a follow-up", and the Founder's 2026-09-19 order says the opposite in as many words — "Parity before demotion is how a second home survives forever. Preserve capability. Kill competing authority." The cut follows the order: AUTHORITY (the peer-home claims) is killed; CAPABILITY (the route, its family membership, its tier-1 ROOM rail door, every deck organ) is preserved untouched, and the deck stays two taps away on a phone via any strip door → Rooms toggle → the 21-door sheet. SUB-ATOM (1), THE ORGAN INVENTORY, IS WRITTEN 2026-09-19 at `docs/operations/DECK-ORGAN-INVENTORY.md` — strict-import census (real `from "..."` statements only, after an earlier mention-counting census was discarded with four of its claims overturned by direct grep: SignalProvenanceStrip and OptionExpressionIntent are SHARED, AvailableRChip's module is shared via `selectAvailableRDetail` in DecisionSpineBand, and StoryRibbon's only non-deck importer is the ORPHANED ConnectedStoryRibbon, which nothing imports). Verdict: 15 DECK-ONLY organs each with a suggested migration target (suggestions, not decisions — every move is its own atom under "preserve capability"), 19 SHARED modules that lose nothing when the deck is quarantined. SUB-ATOM (2), THE QUARANTINE LABELLING, IS IMPLEMENTED 2026-09-19 BUT UNGATED: `wmDestinations.ts` grows `authority?: "legacy"` — a VERDICT field kept deliberately separate from `frame`, which stays a measurement of chrome (bending frame to express the verdict would falsify a fact); exactly one entry carries it, `/command-deck`. Both shells consume it: `WMOperatingSystem.tsx` spreads `{ legacy: true }` into the deck's OS_ROOMS entry (absence-not-false — normal rooms carry NO key) and RailLink renders a LEGACY chip (`data-testid="os-rail-legacy-chip"`); `MainLayout.tsx`'s 72px rail destructures `authority` and renders `data-testid="rail-legacy-chip"`, its Workspace drawer's withheld-while-capital-is-live section (the July shell's SECOND ROOM-door surface) renders `data-testid="drawer-legacy-chip"`, and its stale five-slot phone-nav comment (still naming DECIDE/Command Deck) was rewritten to defer to PHONE_SLOT_HREFS as the owner. The OS phone Rooms sheet needs no extra wiring — it is the same rail, one RailLink renderer. `commandDeckQuarantine.test.ts` pins it four ways: exactly-one-legacy, capability preservation (group/tier/frame unchanged), OS_ROOMS carry with `toBeUndefined()` for every other room, and source breadcrumbs proving both renderers consume the fact. UNGATED MEANS UNGATED: neither vitest nor tsc has run over any of this — the Bash safety classifier has been unavailable across repeated retries this segment, so no exit code exists and nothing here is claimed green, committed, or deployed. WHAT KEEPS THIS ROW OPEN: (a) the gate run + commit + deploy + live observation of both LEGACY chips, and (b) the 15 organ-migration decisions, each its own atom. |
| M4 — legacy tests protecting legacy architecture | CLOSED | `src/components/layout/ShellAccessParity.test.tsx` | 2026-09-18 | REMODELLED IN THE SAME COMMIT AS M1, which is the whole law. The five-door assertions were not wrong and were NOT deleted — they still guard every room where choosing where to go next IS the job. What was missing is that this file rendered the shell exactly ONCE, with no route, so every assertion described the non-instrument shape and nothing here had an opinion about /charts. That is how the two widths disagreed with no gate able to see it. The route is now an input: `usePathname` is mocked, both shapes are rendered, and a new block asserts the instrument view draws no strip AND still reaches more doors than the strip did. Proof: `./node_modules/.bin/vitest run src/components/layout/ShellAccessParity.test.tsx` EXIT 0, 20 passed (was 14). |
| M5 — route registry overload | OPEN | `src/lib/routing/wmDestinations.ts` | 2026-09-18 | PARTLY REFUTED ON MEASUREMENT. `WmDestination` (`:119-143`) already separates its `frame` field (os / cleared / legacy) from its `tier` field (1 or 2), and phone-bar membership is a THIRD list (`PHONE_SLOT_HREFS`, `:373`) rather than being derived from `frame`. The conflation named in the order is not present as described. What is unproven is whether the three concepts stay independent under M1, which is what this row should be re-measured against. |
| M6 — source poison | OPEN | `src/lib/ops/datedDocsAreDemoted.sentinel.test.ts` `src/lib/ops/repoFrontDoorAuthority.test.ts` | 2026-09-19 | **THE DOCS HALF IS CLOSED AND HAS A RUNNABLE OWNER: `npm run test -- datedDocsAreDemoted`.** Mutation-proved 2026-09-19 — clean EXIT 0, unstamping one doc EXIT 1 naming that doc, restore EXIT 0. **TWO CANDIDATE RULES WERE MEASURED AND REJECTED BEFORE THIS ONE WAS WRITTEN,** because the obvious generalisation was worthless in both directions. (1) The literal generalisation — apply `viewBuildOrder`'s per-paragraph "a build-status word must sit beside a date or a SHA" to all 64 undemoted ops docs — measured **70 offending units across 27 docs**, and most were table LEGENDS defining what `PARTIAL` means, template column headers, and prose explaining the rule itself. A gate whose remedy is seventy inline edits to text that was never wrong teaches authors to delete the word `PARTIAL` from a legend. (2) Moving the unit up to the DOCUMENT — require a date in the filename or first twelve lines — measured **1 offender out of 64**, i.e. sixty-three docs passing by accident, which is the vacuous green this suite guards against in three other files. **THE DEFECT WAS ONLY VISIBLE ON A THIRD CUT:** 89 ops docs, 25 demoted, 64 undemoted — and **47 of the 64 carried a date in their own FILENAME**. Forty-seven shift batons, session receipts, audits and checkpoints — point-in-time BY CONSTRUCTION, each announcing its own expiry in its own name — were still speaking in the present tense with no machine-readable demotion stamp. `CODEX_CONTINUITY_2026-08-10_0900.md` tells a reader what to do next; it was right on 2026-08-10. A worker arriving by search result, shared link or directory listing never passes through `README.md` and never meets its demotion notice. That is RETIREMENT_DEBT, the class named in `repoFrontDoorAuthority.test.ts`'s docblock when it was found in the README — nobody had checked the side doors. **THE FILENAME IS THE SIGNAL** because it cannot be argued with and cannot drift: a doc titled with a day has already told every reader it is not current, in the one piece of metadata seen before opening. And the remedy is ONE stamp per file, which is a remedy someone will actually apply. 46 docs were stamped. **THE EXEMPTION IS DECLARED, NOT LISTED:** `CANON-VIEW-BUILD-ORDER-2026-09-17.md` is dated AND genuinely current — `viewBuildOrder.sentinel.test.ts` requires it to claim first-read authority — so it opts out in its own body with `<!-- ath-standing-authority: 2026-09-19 -->`, naming the day it was last re-measured; a bare undated claim is refused, and a fourth claimant fails the gate. An allowlist inside the test file would put the exemption where the document's author never looks. **THE NEW GATE IMMEDIATELY CAUGHT A FALSE NEGATIVE IN ITS OWN ROLLOUT:** `GARDEN_RECEIPT_2026-09-11_P0A_FRONT_DOOR_RETIREMENT.md` — the receipt for the repair that INVENTED the lineage marker — was reported already-demoted by a `grep -l` for the marker's NAME, because it names the marker at line 115 while explaining it, and demotes itself in PROSE at line 3. It had never carried the marker in POSITION. Prose is not a machine. **WHAT KEEPS THIS ROW OPEN:** the 17 ops docs with no date in the filename are outside this instrument's reach and need a different one, and the order's "stale comments and route prose" half — the same rule applied inside `src/` — is not built. |
| M7 — permission-born DECISION_ID persistence | OPEN | `src/lib/traderMemory/decisionMemory.ts` `src/lib/traderMemory/decisionMemoryStore.ts` | 2026-09-18 | A decision minted at permission crossing lives in React state until a later intent write, and the store says in its own words that it cannot survive reload or cross-device. **DO NOT FABRICATE AN INTENT TO PERSIST THE ROW** — that mints a decision no trader made, which is a worse defect than the one being fixed. The seam needed is birth/existence, separate from expression. Shares an owner with the "Decision Memory sealing" row above; close them together or they will disagree. |
| M8 — CanonicalBar adoption + object vocabulary | OPEN | `src/lib/marketData/canonicalBar.ts` `src/lib/marketData/marketObjectKinds.ts` `src/lib/marketData/canonicalBarAdoption.sentinel.test.ts` | 2026-09-18 | VOCABULARY HALF CLOSED `540a2cc5`: the kind is spelled `GAP_FVG`, the wrong total is out of the prose and test title, and a guard now fails if a count is retyped beside the array. Re-run `./node_modules/.bin/vitest run src/lib/marketData/marketObjectKinds.test.ts` — EXIT 0. THE ADOPTION HALF IS STILL FULLY OPEN and is the larger one. MEASURED 2026-09-18, not assumed: the artery has ZERO production consumers — every import of it in this repo is a test importing it to test it — while production declares its own open/high/low/close in 22 other places. Twenty-two shapes is not twenty-two styles, it is twenty-two pasts, because two modules can each hold a different 09:31 for the same symbol and neither is wrong by its own lights. A RATCHET now freezes that census so the sprawl can only SHRINK while the migration runs: `./node_modules/.bin/vitest run src/lib/marketData/canonicalBarAdoption.sentinel.test.ts` — EXIT 0, 4 passed. Two mutation receipts earned: a 23rd OHLC shape appended to `src/lib/timeframes.ts` turned it red on the added branch, a phantom entry in the frozen list turned it red on the removed branch, both restored and re-verified green. THE RATCHET IS NOT THE FIX AND DOES NOT CLAIM TO BE — a green run means only "the sprawl has not grown". This row stays OPEN until a real ingress is migrated and the frozen list is lowered in the same commit. THE FIRST MIGRATION ATTEMPT FOUND A WALL, 2026-09-18, and the wall is the reason adoption is zero rather than neglect being the reason: `CanonicalBar.sessionId` is REQUIRED and NOTHING in this product produces one. The only session known anywhere is `CanonicalMarketEvent.sessionId`, which is OPTIONAL and is populated by exactly one adapter — `webullTicksBrowser.ts` passing through the provider's own `tradingSession`. So every candidate ingress faced two bad doors: invent a session, which launders unknown data into a canonical field and is precisely the failure the deliberately-absent inverse of `toLegacyTuple` exists to prevent; or refuse every non-Webull bar, which amputates the live chart. A THIRD DOOR IS NOW BUILT: `SESSION_UNKNOWN` plus `isSessionKnown`, an assertion of ignorance that travels with the bar. `admitBar` now REFUSES a bar that claims EXECUTABLE fidelity while its session is unknown — a price is a different fact inside RTH than outside it — and it refuses rather than silently downgrading, because a quiet correction hides from the caller that the house disagreed with it. Unknown-session bars at the four non-executable fidelities are still admitted, so the chart keeps drawing. `./node_modules/.bin/vitest run src/lib/marketData/canonicalBar.test.ts` — EXIT 0, 27 passed (was 22). One mutation receipt: disabling the guard turned exactly the refusal test and the no-silent-downgrade test red, both restored. STATED PLAINLY SO NOBODY READS THIS AS PROGRESS IT IS NOT: THE CENSUS DID NOT MOVE. It still holds at twenty-two. This commit removed the blocker that every ingress migration would have hit; it migrated nothing. THE CENSUS HAS NOW MOVED, 2026-09-18: TWENTY-TWO TO TWENTY-ONE, and the frozen list was lowered in the same commit that retired the shape, which is the condition this row set for itself. RETIRED: `hooks/useWebSocket.ts::OHLCVBar`. It was a verbatim six-field copy of `liveBarPolicy.ts::LiveBar` — the very module that produced every value the hook ever stored in it — so this was not a disagreement waiting to happen, it was two names for one shape in a file that already imported the producer. The hot-path ref is now typed `LiveBar`, which is what it actually holds, and the PUBLISHED bar is typed `LegacyOhlcvTuple`, the artery's one sanctioned legacy name, which exists precisely so files that still speak six bare numbers have something to be adapted FROM instead of a reason to redeclare. THE SMALLER CLAIM IS THE TRUE ONE AND IS RECORDED HERE SO A LATER READER CANNOT INFLATE IT: this did NOT give the live path canonical IDENTITY. The published bar still carries no symbolId, no sessionId, no fidelity, no provenance and no truthEpoch. A duplicate past was deleted; identity on the live path is still owed, and that is why this row stays OPEN. FOUR MORE IMPRESSIVE REPAIRS WERE REJECTED AS THEATER OR AS NEW DEFECTS, named here so they are not re-attempted: routing live ticks through `admitBar`, which is a collision resolver rather than an in-place updater and refuses an equal `truthEpoch` at the same `asOf`, so it would have refused every tick after the first; manufacturing a CanonicalBar and immediately discarding it; adding a canonical field no consumer reads; and mixing units across one interface, since `applyTickToLiveBar` buckets in SECONDS while `receivedAt` is milliseconds. A SECOND-WRITER BUG FELL OUT OF THE RETYPE and was fixed in the same change: `MainChart.tsx` was assigning straight into the published bar's high, low and open — mutating an object React had already handed out from the hook's state, a second writer to someone else's state that the hook could not see. Making the published bar readonly surfaced it; three locals now carry the garbage-OHLC repair with identical numbers and one writer. Mutation receipt on the ratchet: removing the census entry while the shape still existed turned it red on the removed branch with the scoreboard message, restored and re-verified. A GATE WAS REPAIRED RATHER THAN ITS LEDGER TRIMMED, and this is the part most worth reading: typing the published bar via `import type` made `screenReach.enforcement.test.ts` report `canonicalBar.ts` as newly REACHED, and its own failure message invited the flattering fix — delete the ledger entry so the count stays honest. That would have recorded the artery as having a screen because one TYPE crossed a boundary. A type-only import is ERASED, not tree-shaken, so the emitted bundle contains no reference to the module at all; the walk now strips whole type-only statements before counting edges, deliberately narrow enough that an INLINE type modifier still counts as reach because such a statement still emits its module for its value binding. The repair paid for itself immediately by exposing a pre-existing overclaim: `src/lib/broker/BrokerAdapter.ts` has seven exports, all of them types or interfaces, and all nine of its importers reach it with `import type`, so it ships no runtime code to any screen and had been mis-credited as reached. It is now on the ledger under a NEW reason, `TYPE_ONLY`, which is the one reason in that vocabulary that is not a debt — a contract emitting nothing cannot be wired to a surface, and filing it as AWAITING_SURFACE would invite someone to finish it by adding a runtime export it has no reason to have. The exclusion is itself guarded by five explicit source-text cases including the inline-modifier case a naive regex gets wrong, because deleting that one replace call would have broken no assertion on the day it was deleted. Gates: `./node_modules/.bin/tsc --noEmit` EXIT 0 and `./node_modules/.bin/vitest run` EXIT 0 at 807 files, 10202 passed, 2 skipped — and the total MOVED by exactly the one test added, which is the check that the new guard is genuinely being collected rather than silently skipped. TWENTY-ONE TO TWENTY, 2026-09-18, AND THIS ONE IS A SMALLER CLAIM THAN THE LAST: `types/index.ts::OHLCVBar` is retired by DELETING THE WHOLE FILE IT LIVED IN, because that barrel had ZERO importers. Not few — none. Nothing under src or anywhere else in the repo referenced `@/types` or `src/types/index`, there is no `export *` anywhere that could have re-exported it, and the only path alias is `@/*` to `./src/*`, so the resolution had nowhere else to come from. The sibling `@/types/chart` IS imported and is untouched. Two of the barrel's twelve exports, `Post` and `SignalStrength`, had already been RE-DECLARED inside the files that wanted them — `app/lounge/page.tsx` and `SmartMoneyPanel.tsx` — which is the signature of a barrel that was abandoned rather than one still being written toward. THE WHOLE FILE WENT AND NOT JUST THE COUNTED LINE, deliberately: excising one interface and leaving eleven other unreferenced ones behind would have moved this number while leaving the actual poison in place, since a future reader greps OHLCVBar, finds `@/types`, and learns that importing a bar from there is a thing this product does. That would have been a change made for the scoreboard rather than for the code. STATED SO NOBODY READS IT AS MORE THAN IT IS: this migrated no ingress. It removed a declaration no ingress was using — dead source the census had been counting as if it were live — so of the two steps from twenty-two to twenty, only the FIRST touched running code. NO CAPABILITY WAS AMPUTATED because there was no capability: an interface nothing imports has no consumer to migrate, parity with nothing is what tsc and the full suite staying green demonstrate, and git history keeps the text. One stale sentence in the screen-reach ledger that named `types/index.ts` as a live duplicate was corrected in the same commit, which is the M6 rule applied to a file this very commit deleted. Mutation receipt: re-adding the census entry while the file stayed deleted turned exactly one test red on the removed branch with the scoreboard message, restored and re-verified EXIT 0. Gates after deletion: `tsc --noEmit` EXIT 0 and `vitest run` EXIT 0 at 807 files, 10202 passed, 2 skipped — the total correctly did NOT move, because this change adds and removes no test. LIVE OBSERVATION 2026-09-18, AND WHAT IT CANNOT REACH IS NAMED FIRST. The `MainChart` de-mutation is the only part of this work that changes running behaviour, and it is the part production cannot currently prove, for two independent reasons. One: it introduces no new string. Renaming three reads into three locals compiles to the same minified arithmetic, so the bundle-scan channel that proved M9 DEPLOYED has nothing to look for here and would be dishonest if forced to produce something. Two: the fold it guards only runs on a live tick, and the observation was taken at 21:13 Central on a Friday with the equity session closed, so no tick was available to fold. WHAT WAS ACTUALLY OBSERVED, and it is a weaker claim than either of those: a NEW BUILD IS BEING SERVED — refetching /charts returns a chunk set that differs from the one the Founder's already-open tab has loaded, one chunk added and two gone, which is what a rebuild of changed modules looks like — and /charts, /readiness and /journal all return HTTP 200 with full shells, so nothing in this work broke the build or the route. THE FOUNDER'S SESSION WAS NOT TOGGLED AND HIS TAB WAS NOT RELOADED to manufacture a better-looking receipt. THE HONEST STATE IS THEREFORE: the de-mutation is REASONED AND UNIT-COVERED, the route is SERVING, and the live-bar fold is UNOBSERVED — it needs a session with real ticks, and until one is available this row will not say otherwise. THEN THE CENSUS WENT FROM TWENTY TO EIGHTEEN WITHOUT ANYTHING BEING RETIRED, and that is recorded as a MEASUREMENT CORRECTION rather than progress because it is not progress. A probe over every declaration the matcher was counting reported the declared type of each of the four fields, and two of the twenty were not records of a bar at all - lib/pine/interpreter.ts::ExecContext declares open high low close as number[], the Pine interpreter's execution frame holding the WHOLE series as parallel columns, of which there is exactly one per script run and which cannot hold a competing 09:31; and lib/chart/dataWindowBarScope.ts::DataWindowBarScope declares them as DataWindowCell, which is a label and a title, two strings for a panel, carrying no price whatsoever. BOTH SHAPES ARE STILL IN THE PRODUCT, UNCHANGED, EXACTLY WHERE THEY WERE - nothing was migrated and nothing was deleted for this step, and eighteen was always the true size of M8. THE FLATTERING REPAIR WAS AVAILABLE AND WAS REFUSED: name-excluding those two entries would have produced the same eighteen while leaving the matcher still wrong, so the next open: number[] anyone declares would have re-inflated the census with a shape nobody can migrate. The predicate was narrowed instead to require a scalar number with a negative lookahead rejecting the array, and because the narrowing IS the matcher it got a guard test of its own built from explicit source text rather than from real files, since a case pointed at interpreter.ts stops testing the predicate the day that file is edited for an unrelated reason. TWO MUTATION RECEIPTS WERE EARNED. Stripping the negative lookahead failed exactly two tests, the guard on the line that says columns of a whole series cannot hold a competing 09:31 and the census on its ADDED branch re-listing ExecContext, which is both halves of the defect reappearing together. Restoring one removed entry failed exactly one test on the REMOVED branch naming lib/pine/interpreter.ts::ExecContext, which is the scoreboard rule working - and that rule is precisely why this correction is written down at all, because two entries leaving with no note would read as two migrations that never happened, the exact inverse of the failure the freeze was built to prevent and the more flattering one. tsc --noEmit EXIT 0 and vitest run EXIT 0 at 807 files with 10203 passed and 2 skipped, the total moving from 10202 by exactly the one test added, which is the check that the new guard is genuinely collected rather than silently skipped. THE NUMBER WENT DOWN AND THE WORK DID NOT: eighteen ingresses still decide for themselves what a bar is, and the live path still carries no symbolId, sessionId, fidelity, provenance or truthEpoch. NEXT ATOM MEASURED AND THE OBVIOUS TARGET REFUTED: lib/marketData/selectAbsorptionAnatomy.ts declares TWO shapes in one file, AnatomyBar and AnatomyBarInput, which looked like the same verbatim-duplicate pattern already retired once in useWebSocket.ts. IT IS NOT. AnatomyBarInput is an ingress BOUNDARY - the shape a caller must build before the selector will measure anything - and AnatomyBar is that selector's OUTPUT, carrying effort, effortNorm, delta and displacement that the input does not have. There is no cheap retirement at this joint and none was manufactured. WHAT THE SCAN DID FIND IS A REAL DUPLICATION IN RUNNING CODE: ChartsDashboard.tsx built the AnatomyBarInput array TWICE from the same chartBars, two verbatim-identical map blocks, one feeding the Absorption view and one feeding the Aggression Response scatter. The comment above the second promised the two surfaces can never disagree about which bars absorbed - a promise that was true only for as long as two separate blocks of code happened to still agree, which is not a guarantee but a coincidence with a maintenance schedule. The first person to change the volume fallback or the time coercion in one copy and not the other makes two surfaces read two different windows while both claim to read the chart's. They now share ONE anatomyInput memo, so the guarantee is structural. MAINCHART WAS DELIBERATELY LEFT ALONE: it builds its own AnatomyBarInput but that mapping is NOT a duplicate - it reads a real per-bar sub-profile and carries genuine askVol and bidVol off the tape, where the dashboard's are honestly null because OHLCVBar has no aggressor split. Collapsing those two would be the flattering kind of DRY, pretending a measured split and an absent one are the same ingress. TWO LEGACY TESTS BROKE AND WERE REMODELLED IN THE SAME CHANGE, which is M4 arriving inside an M8 atom: absorptionViewIsOfferedEverywhere and aggressionViewIsOfferedEverywhere each asserted the memo literally closed with the four characters of a chartBars dependency array, a PROXY for the fact rather than the fact, and both would have failed a change that made the guarantee STRONGER. They now trace the whole chain - chartBars to anatomyInput to view model - and the aggression sentinel gained a separate assertion that the scatter still must not be derived from the Absorption view model, which is the half of the original comment's intent that sharing an input does not cover. THREE MUTATION RECEIPTS WERE EARNED. Re-introducing the duplicate map failed the new count sentinel and the shared-consumption assertion together. Changing the shared memo's dependency array to empty failed both remodelled sentinels on the line that says anatomyInput re-measures when chartBars changes, which proves the remodel did not weaken what the proxy was protecting. tsc --noEmit EXIT 0 and vitest run EXIT 0 at 808 files with 10207 passed and 2 skipped, the total moving from 10203 by exactly the four tests added. THE SMALLER CLAIM IS THE TRUE ONE: this removed a duplicate DECISION, not a private past. The census is still eighteen and chartBars still carries no symbolId, sessionId, fidelity, provenance or truthEpoch, so the two surfaces now agree about a window that still has no identity. NOT LIVE-VERIFIED: this change has no observable string and needs /charts with real bars in a session the Founder is driving. LIVE OBSERVATION, NAMING WHAT IT CANNOT REACH FIRST: the shared-mapping change has NO observable string - collapsing two identical map blocks into one memo minifies to equivalent code - and it was pushed minutes before this reading, so the new build is NOT confirmed deployed and this row does not say it is. WHAT WAS ACTUALLY SEEN, on the PRIOR build, in the Founder's own already-open session on /charts: the VIEW bar carries both Absorption and Aggression as real destinations; the header reads TSLA 364.30 LAST 5m BAR CLOSE with HISTORICAL BARS VERIFIED and a full OHLCV row; and the SESSION line reads that Sweep and Absorption had every input they need and did not match, which is a per-bar measurement returning a real negative rather than a placeholder. That is genuine evidence the Absorption selector is measuring live bars on the surface this atom feeds - it is NOT evidence that the shared input memo is the thing doing the feeding, because the tab is on the old bundle. /charts, /readiness and /journal all return HTTP 200. THE FOUNDER'S TAB WAS READ AND NOT RELOADED, AND NO VIEW TAB WAS CLICKED, so nothing in his session was toggled to manufacture a better-looking receipt. THE HONEST STATE IS: the refactor is REASONED, UNIT-COVERED AND MUTATION-RECEIPTED, the routes are SERVING, the surface it feeds is OBSERVABLY MEASURING on the prior build, and the new bundle is UNOBSERVED.  2026-09-18 RETIREMENT 3 - lib/markov.ts::Bar is gone and the census is SEVENTEEN. The Markov engine declared six fields, all number, byte-for-byte identical to LegacyOhlcvTuple in the artery - the shape whose own docblock says it exists so files that speak it have somewhere to be adapted FROM rather than a reason to redeclare it. classifyBar and the bars input now take LegacyOhlcvTuple, and markov.test.ts - the module's ONLY importer in the whole repo - imports the same name. NO ALIAS WAS LEFT BEHIND: a re-export of Bar from markov.ts would have made this row read seventeen while the product still spoke eighteen names. WHY THIS MODULE FIRST: one importer and that importer is its own test file, so the blast radius is two files and no surface moves - the cheapest available proof that the rename path works before it is walked across the heavier modules behind it. THE FINDING THAT REFRAMES M8, measured this iteration and written into the sentinel header: THIRTEEN of the seventeen remaining private pasts are byte-for-byte the same six fields as LegacyOhlcvTuple, and a fourteenth differs only by an optional volume. The sprawl is not seventeen competing ideas of what a bar is - it is one anonymous six-field tuple wearing a dozen module-local labels, with a sanctioned name for exactly it already in the artery. THAT MAKES MOST OF THE REST A RENAME, NOT A MIGRATION, AND THE EASE MUST NOT FLATTER THE RESULT: renaming thirteen declarations removes thirteen duplicate DECISIONS and delivers ZERO canonical identity. LegacyOhlcvTuple is the legacy shape on purpose - no symbolId, no sessionId, no fidelity, no provenance, no truthEpoch. The Markov engine still counts transitions over a past that cannot say which symbol or session it belongs to. The adoption half of M8 is untouched and stays owed. MUTATION RECEIPT: restoring lib/markov.ts::Bar to the frozen array produced EXIT 1 with exactly one failing test - holds at seventeen private pasts and may only SHRINK - on the REMOVED branch, naming lib/markov.ts::Bar in its message; restored, EXIT 0. GATES: tsc --noEmit EXIT 0 and vitest run EXIT 0 at 808 files with 10207 passed and 2 skipped. THE TOTAL DID NOT MOVE FROM 10207 AND THAT IS THE CORRECT OUTCOME HERE - no test was added and none was deleted; an unmoved total is only a signal when a test was supposed to arrive. NOT LIVE-VERIFIED: markov.ts is behind NEXT_PUBLIC_MARKOV_ENGINE=v1 and has no production surface (screenReach records it AWAITING_SURFACE), so there is no screen on which this rename could be observed. Claiming a live proof for it would be fabrication. | SEVENTEEN TO THIRTEEN 2026-09-18: four more private bar shapes stopped being private names and now speak `LegacyOhlcvTuple` — `app/api/exchange/route.ts::Bar`, `components/chart/WatchlistGrid.tsx::Candle`, `lib/api/kraken.ts::KrakenOHLC`, `lib/yahooCandleConsumer.ts::YahooCandle`. THESE FOUR WERE ONE ATOM FOR A MEASURED REASON, not for convenience: every remaining census entry was measured for external importers and these were exactly the four with ZERO importers outside their own module, so collapsing each changes one file's vocabulary and no other module's agreement. Contrast `components/chart/indicators.ts::Bar` (7 importers) and `lib/pine/types.ts::OHLCVBar` (7), which are a different risk class and are NOT in this atom. NO ALIAS WAS LEFT BEHIND in any of the four. TWO OF THEM WERE THE PROVENANCE-IN-THE-NAME CASE: `KrakenOHLC` and `YahooCandle` announced a venue in the identifier while carrying no `source` field, so a Kraken row and a Yahoo row were freely assignable to each other's names and neither type could object; the rename stops the false claim but does NOT supply the provenance, which is CanonicalBar's job and CanonicalBar still has zero production consumers. MAINCHART WAS DELIBERATELY EXCLUDED although it too has zero importers: `components/chart/MainChart.tsx::Bar` has ~50 references in the live-chart hot path including a `useRef` the tick handler MUTATES, and `LegacyOhlcvTuple` declares all six fields `readonly`, so that rename asks a real question about whether the live path writes into its own bars — its own atom, not a ride on this one. THE HONEST ARITHMETIC: twenty-two to thirteen is eight renames-or-deletes plus one measurement correction and ZERO INGRESSES MIGRATED. Nine of the thirteen survivors are still byte-for-byte the same six fields. Mutation receipt: restoring `lib/api/kraken.ts::KrakenOHLC` to the frozen array produced EXIT 1 with exactly one failure, `holds at thirteen private pasts and may only SHRINK`, on the REMOVED branch naming that entry; restored, EXIT 0. Gates: `tsc --noEmit` EXIT 0 (no `readonly` collision in any of the four) and `vitest run` EXIT 0 at 808 files with 10207 passed and 2 skipped. THE TOTAL DID NOT MOVE FROM 10207 AND THAT IS AGAIN THE CORRECT OUTCOME: no test was added or removed, only a frozen list shortened. NOT LIVE-VERIFIED AS A RENAME: a type alias has no runtime representation, so there is no pixel on which `LegacyOhlcvTuple` could be observed differing from `Candle`; the only honest live claim available is that the surfaces fed by these modules still render, and `tsc` plus the suite is the proof that actually bears on the change. THIRTEEN TO TWELVE 2026-09-18: `components/chart/MainChart.tsx::Bar` retired, the one the previous row deliberately held back. IT WAS HELD BACK FOR A REASON AND THE REASON WAS REAL: `LegacyOhlcvTuple` declares all six fields `readonly` and the chart de-spikes wicks by assigning to `.high` and `.low`. MY FIRST READING OF THAT WAS WRONG AND IS RECORDED AS WRONG: reading the call sites said the objects are locally owned so nothing is at risk, which is true about OWNERSHIP and says nothing about whether it compiles. `tsc` rejected FOUR lines across two de-spike passes and named each one. The repair was not a retreat -- both clamps now REPLACE the bar instead of editing it, which is the shape they should have had, because a bar that can be edited after publication is exactly how a corrected value silently replaces the one a trader already acted on, and preventing that is what truthEpoch is for. THE OWNERSHIP READING WAS RIGHT ABOUT THE THING THAT MATTERS: no write reached back into `barsRef.current`. The chart's stored history is append-only in practice, and the type system now enforces it rather than a convention. That is one real precondition for canonical identity on the live path, established -- and only a precondition; the bars drawn here still carry no symbolId, sessionId, fidelity or provenance. BLAST RADIUS 72 REFERENCES, and one was a trap: `barsRef.current as IND.Bar[]` names the type `indicators.ts` exports, not the chart's own; a blanket rename rewrote it and `tsc` caught that too. Restored and commented, since retiring `indicators.ts::Bar` with its seven importers is its own atom. Mutation receipt: restoring `components/chart/MainChart.tsx::Bar` to the frozen array produced EXIT 1 with exactly one failure, `holds at twelve private pasts and may only SHRINK`, on the REMOVED branch naming that entry; restored, EXIT 0. Gates: `tsc --noEmit` EXIT 0 and `vitest run` EXIT 0 at 808 files with 10207 passed and 2 skipped -- unmoved again, correct again, no test added or removed. TWENTY-TWO TO TWELVE IS NINE RENAMES-OR-DELETES PLUS ONE MEASUREMENT CORRECTION AND ZERO INGRESSES MIGRATED. ATOM 4 (2026-09-18): `lib/backtest/engine.ts::Bar` and `lib/timeframes.ts::Candle` retired; census 12 -> 10. Both byte-for-byte LegacyOhlcvTuple, each with exactly ONE importer which is the module's own test file. NO ALIAS LEFT BEHIND; both tests migrated in the same change. tsc clean first try -- neither module assigns to a bar field, so readonly cost nothing. HONESTY CORRECTION TO ATOM 2's ROW: that row called indicators.ts::Bar (7 importers) and pine/types.ts::OHLCVBar (7) a different risk class on raw count. Re-measured: of indicators.ts::Bar's 7, five are its own test files, one is selectMarketStructure.test.ts, and exactly ONE is production. Of pine/types.ts::OHLCVBar's 7, three are production. Raw importer count conflates test importers with production ones and overstated the risk. NAMED BUT NOT CLOSED: timeframes.ts::aggregateCandles MANUFACTURES bars that never came from a provider (it folds N into one) and neither the old local Candle nor the shared legacy tuple has anywhere to record that a folded bar has a different fidelity from a fetched one. Written into the source at the declaration site. MUTATION RECEIPT: restoring lib/backtest/engine.ts::Bar gave EXIT 1 with exactly one failure -- holds at ten private pasts and may only SHRINK -- naming that entry; restored, EXIT 0. GATES: tsc --noEmit EXIT 0; vitest run EXIT 0 at 808 files with 10207 passed and 2 skipped, unmoved and correctly so because no test was added or removed. NOT LIVE-VERIFIED AS A RENAME: a type alias has no runtime representation, so there is no pixel on which LegacyOhlcvTuple could be observed differing from Bar or Candle. STILL OWED: zero ingresses migrated; symbolId, sessionId, fidelity, provenance and truthEpoch remain absent on the live path. ATOM 5 (2026-09-18): liveBarPolicy::LiveBar, sessionVP::Candle and yahooTimeframes::YahooOhlcvBar retired; census 10 -> 7. All three byte-for-byte LegacyOhlcvTuple. THE NO-ALIAS RULE EARNED ITS KEEP HERE: because each module stopped EXPORTING a bar type instead of re-exporting one, tsc failed five importers with TS2459 and every one now imports from the artery directly. liveBarPolicy is the consequential one -- it is ON THE LIVE PATH (every websocket tick goes through applyTickToLiveBar) and it already enforces that a late event may not rewrite a bar, which is the same instinct truthEpoch formalises, but it enforces it on a shape with no truthEpoch to enforce against. yahooTimeframes was the third and last provenance-in-the-name case and the sharpest, because that module RECONSTRUCTS bars: several plans are sourceMode reconstructed, so bars the caller receives were folded from a finer interval and never traded at the requested timeframe anywhere. sourceMode is known at the planner and there is nowhere on the bar to put it. TWO LEGACY TESTS REMODELLED IN THE SAME CHANGE per M4: the FALSE_RIPENESS floor asserted the raw scan found MORE THAN TEN declarations, a number calibrated to a bigger census that would have failed the moment the migration succeeded -- it is a matcher-liveness check not a size assertion and is now floored at three; and screenReach.enforcement.test.ts used an import statement as a parser fixture naming LiveBar, which would have stayed green while teaching the retired noun. MUTATION RECEIPT: restoring liveBarPolicy.ts::LiveBar gave EXIT 1 with exactly one failure -- holds at seven private pasts and may only SHRINK -- naming that entry; restored, EXIT 0. GATES: tsc --noEmit EXIT 0; vitest run EXIT 0 at 808 files with 10207 passed and 2 skipped, unmoved. LIVE VERIFICATION, STATED HONESTLY FOR THIS ATOM AND FOR eb9cbc0c: /charts was reloaded in the Founder's Chrome after eb9cbc0c's CI went green and renders correctly -- 9 canvases, main chart 1564x616 CSS px, price line TSLA 364.30 LAST 5m BAR CLOSE, HISTORICAL BARS VERIFIED true, NO FEED absent, no React error. That is a NO-REGRESSION observation and NOT proof the new bundle is serving: the repo has only sentinels.yml and deploys run through Cloudflare's own git integration, which gh cannot query, and the App Router page exposes no buildId to correlate. Separately, the de-spike rewrite is observationally SILENT by construction -- replace-versus-mutate produces identical numbers -- so no pixel could distinguish the two builds even with a confirmed deploy. Recorded as NOT PROVEN rather than dressed up. STILL OWED: zero ingresses migrated; symbolId, sessionId, fidelity, provenance and truthEpoch absent on the live path. ATOM 6 (2026-09-18): lib/vpEngine.ts::ProfileBar retired; census 7 -> 6. Byte-for-byte, four namers of which two are production -- chartMarketStatePublisher.ts and viewModels/selectLivingProfile.ts. WHY THIS ONE IS MORE THAN A RENAME TO NOTE: a volume profile is a claim about how much traded at each price during a window, and it gets PUBLISHED as market state. The shape it is computed from cannot say which session the window belongs to, at what fidelity the volume was observed, or whether a later correction supersedes it. The publisher ships the result anyway because there is nothing on the bar to gate it with. That is the adoption half of M8 stated as a product consequence rather than a list of missing fields. MUTATION RECEIPT: restoring lib/vpEngine.ts::ProfileBar gave EXIT 1 with exactly one failure -- holds at six private pasts and may only SHRINK -- naming that entry; restored, EXIT 0. GATES: tsc --noEmit EXIT 0; vitest run EXIT 0 at 808 files with 10207 passed and 2 skipped, unmoved. NOT LIVE-VERIFIED: type-alias rename, no runtime representation. ATOM 7 2026-09-18: retired lib/pine/types.ts::OHLCVBar, byte-for-byte LegacyOhlcvTuple, eight namers of which three are production (ChartsDashboard.tsx, CustomIndicatorBuilder.tsx, lib/pine/interpreter.ts). Census SIX to FIVE. NO ALIAS LEFT BEHIND. WHY THIS ONE MATTERS BEYOND THE COUNT: it is the only shape in the census consumed by code a TRADER wrote. interpretPine hands six anonymous numbers to a user-authored script and cannot tell that script which symbol, which session, at what fidelity, from what source, or whether a correction superseded them, so a script reading close cannot know it is reading a RECONSTRUCTED bar. STILL ONLY A RENAME and that gap stays open, named at pine/types.ts and in the census docblock. M4/M6 IN-BAND: fourteen files carried comments teaching OHLCVBar as a live noun (OHLCVBar.time is in SECONDS and similar) which no type-checker reads and which would have stayed green teaching a retired name - all remodelled in the same atomic change. THREE files naming an EARLIER, different retired OHLCVBar as HISTORY (this census, canonicalBar.ts, useWebSocket.ts) were deliberately NOT rewritten because those sentences remain true. GATES: tsc --noEmit EXIT 0 first try, vitest run EXIT 0 unpiped at 808 files 10207 passed 2 skipped - total UNMOVED and correct, no test was added or removed. MUTATION RECEIPT: restoring the retired entry gave EXIT 1 with exactly one failure on holds at five private pasts and may only SHRINK, restored, EXIT 0. LIVE: NOT PROVEN - a type alias has no runtime representation, so no pixel can distinguish this build from the last. ATOM 8 2026-09-18: retired components/chart/indicators.ts::Bar, the LAST byte-for-byte LegacyOhlcvTuple in the census. Ten namers of which exactly TWO are production (MainChart.tsx, viewModels/selectMarketStructure.ts). Census FIVE to FOUR. Left for last on purpose: that file says the bare word Bar SIXTY times including in English prose, so a blanket rename was unsafe until each occurrence was classified as type or sentence. THE COMPILER OVERRULED ME AGAIN AND THE RECORD SAYS SO: I wrote a MainChart comment asserting the cast could stay as IND.LegacyOhlcvTuple because indicators.ts re-exports the artery type. It does not and MUST NOT - that is exactly the ALIAS LEFT BEHIND this census forbids - and tsc answered TS2694 Namespace has no exported member. MainChart now names the artery through the artery. WHAT IT BUYS: zero canonical identity, as every rename in this block has. The consequence worth naming here is that vwap, cvd, obv, mfi and every other volume-weighted function in this file compute a number only as meaningful as the FIDELITY of the volume handed to them, and no parameter type can say whether that volume was observed trade-by-trade, folded from a coarser feed, or reconstructed for a timeframe that never traded. Contrast PivotBar in the same file: narrowing a requirement is real work, renaming a duplicate is not. MILESTONE: the DUPLICATE-ELIMINATION half of M8 is DONE - no census shape is byte-for-byte LegacyOhlcvTuple any more - and NOT ONE INGRESS HAS BEEN MIGRATED. canonicalBar.ts still has ZERO production consumers. Twenty-two to four with the artery as unused as it was at twenty-two. GATES: tsc --noEmit EXIT 0, vitest run EXIT 0 unpiped at 808 files 10207 passed 2 skipped - total UNMOVED and correct, no test added or removed. MUTATION RECEIPT: restoring the retired entry gave EXIT 1 with exactly one failure on holds at four private pasts and may only SHRINK, restored, EXIT 0. LIVE: NOT PROVEN - a type alias has no runtime representation. ATOM 9 2026-09-18 — THE ARTERY HAS ITS FIRST PRODUCTION CONSUMER. /api/yahoo candles now mint CanonicalBars via lib/marketData/yahooCandleIngress.ts; provenance comes from the planner's own sourceMode so a reconstructed 4h bar is DERIVED, not something a venue printed. sessionId is SESSION_UNKNOWN and fidelity INDICATIVE because Yahoo does not report a session — an assertion of ignorance, not a placeholder. Geometry is now CHECKED not trusted and a repeated timestamp is refused, neither of which this path did before. Wire shape unchanged. INDEPENDENT CORROBORATION: screenReach.enforcement.test.ts, which shares no code with the census and ignores type-only edges, failed in the same run with 'listed as unreachable but a screen now reaches them' and its canonicalBar.ts ledger entry was deleted per its own stated exit condition. A first mutation receipt caught the NEW adoption ratchet passing while broken, because route.ts also type-imports the artery and type imports are erased; the gate now demands a RUNTIME edge and two further receipts prove it bites. Gates tsc EXIT 0, vitest EXIT 0 at 809 files 10222 passed 2 skipped, up 15 for 15 added tests. NOT PROVEN live: observationally silent except on refusal, and the deploy cannot be correlated to a SHA. ATOM 10 (2026-09-18): SECOND INGRESS. /api/exchange now mints CanonicalBars for all five public crypto venues. Chosen because it is where an unchecked bar is most likely to be wrong and hardest to see: Coinbase publishes time,low,high,open while Kraken publishes time,open,high,low with volume at a different index, five hand-written mappings, and nothing downstream checked any of them. A transposed index throws nothing, type-checks, and renders a full chart of inside-out candles on one venue only; checkBarGeometry refuses and counts it. NEW TRUTH THE ARTERY COULD NOT PREVIOUSLY SPEAK: SESSION_CONTINUOUS, which is NOT a tidier SESSION_UNKNOWN. A crypto book has no open, close or pre-post, so the session question has a real answer and the answer is none. That deliberately retires admitBar's EXECUTABLE guard for continuous venues (its stated objection, that a price differs inside RTH vs outside, cannot arise where there is no outside) while these bars stay INDICATIVE for a separate reason that still binds: no execution adapter routes through a public REST proxy. FOUND WHILE BUILDING: venue must be part of crypto instrument identity, else Coinbase BTC 15:00 and Kraken BTC 15:00 mint the SAME barId and admitBar silently discards the second as a redelivery; symbolId is now COINBASE:BTC. Also found: the naive venue prefix returned COINBASE: for a blank coin, smuggling emptiness past mintBarId's check. Also found and fixed IN THE GATE: the adoption ratchet's regex gap could span statement boundaries, assembling a fake runtime edge from two unrelated imports; a mutation receipt confirms the loosened form passes against a genuinely broken route while the tightened form fails. A further receipt proves the new edge bites. The census array did NOT move and should not have. The candles wire shape is unchanged. Gates tsc EXIT 0, vitest EXIT 0 at 810 files 10242 passed 2 skipped, up 20 for 20 added tests. NOT PROVEN live at time of writing. ATOM 11, 2026-09-18, THE THIRD INGRESS. /api/alpaca now mints CanonicalBars through `alpacaCandleIngress.ts`. /api/memecoin was rejected on evidence, not size: it is classified DARK in apiEndpointsHaveConsumers.test.ts with zero callers, and growing a may-only-grow ratchet with an edge no live request traverses is a rename wearing a migration costume. Four defects removed. One, the volume-fallback-to-zero fabrication survived in the candles branch after the SAME family was killed twice in the quote branch of the SAME file; production was measured first so the fix amputates nothing, TSLA 15m had zero absent volumes and BTC 15m had one genuine zero which still draws. Two, the timeframe that lies: probed live, tf=6M returned 75 bars spaced 30.4 days apart, so canonical timeframe is now derived from the RESOLVED bucket and never the request, which also stops seven request spellings minting seven ids for one physical monthly bar. Three, session now varies per asset class and is not a guess: crypto is CONTINUOUS, stocks are UNKNOWN, and neither half claims EXECUTABLE for two different reasons. Four, the first collision between two MIGRATED ingresses: ALPACA:BTC/USD versus COINBASE:BTC would have minted one id and silently deleted a real bar from a real venue. A third hole in my own gate was found by mutation: the regex pinned one historical SPELLING rather than the defect class, so a rewritten fabrication passed, including the logical-or default variant. Broadened and re-proven against four mutations. The prose trap sprang a second time, so a shared codeOnly() comment stripper now serves every negative assertion. Gates tsc EXIT 0, vitest EXIT 0 at 811 files 10265 passed 2 skipped, up 23 for 23 added tests. NOT PROVEN live at time of writing. ATOM 12, 2026-09-18, THE FOURTH INGRESS. /api/finnhub now mints CanonicalBars through finnhubCandleIngress.ts. It carried a fabrication WORSE than the `?? 0` family: high falling back to Math.max(open, close) and low to Math.min does not default a VALUE, it manufactures a GEOMETRY, a candle with NO WICK, which is exactly what a rejection or absorption reader is looking at and is indistinguishable on screen from a real wickless print. Second, the loop checked two of six fields and then dropped the bar with no count and no record: a silently dropped bar is an INVISIBLE gap, the chart simply has fewer candles than the market printed and nothing says so. Both are now counted, disclosed refusals. Third, this is the first ingress whose PROVIDER speaks epoch SECONDS while CanonicalBar speaks MILLISECONDS, both bare numbers, so the conversion is now stated explicitly in both directions. Fourth, identity is minted from the RESOLVED provider symbol and not the request, because a request naming USD is answered from BINANCE:BTCUSDT and USDT is not USD; filing that bar under a USD name would give the label and the number different owners. Fifth, a column SHORTER than the timestamp column is now refused as a misaligned response rather than read as a bar with missing fields. Five mutation receipts, wick repair, silent drop, logical-or volume default, identity from the request, and a type-only import, each EXIT 1, restore EXIT 0. Gates tsc EXIT 0, vitest EXIT 0 at 812 files 10292 passed 2 skipped, up 27 for 27 added tests. NOT PROVEN live at time of writing. LIVE PROVEN 2026-09-18 for ATOM 11: /api/alpaca?type=candles&sym=TSLA&tf=6M answered requestedTf 6M with returnedTf 1M, so the timeframe lie is now disclosed on the live edge rather than inferred from a bar count, and sym=TSLA versus sym=BTC DISAGREE on sessionKnown and sessionModel from the same deployed code, which is what proves alpacaSessionModel is executing per request rather than a constant merely appearing. ATOM 12 remains NOT PROVEN and the reason is a finding, not an excuse: measured on production the same Finnhub token answers type=quote and type=search with HTTP 200 and answers type=candles with HTTP 403 on BOTH stock and crypto. The credential is good and the CAPABILITY is not on the plan, so upstream refuses before any of the new ingress code executes. Deliberately NOT reclassified to a not-entitled edge: finnhubUpstreamStatus.ts forbids that in its own words because no single HTTP status is evidence about a data licence, and that law is correct. The evidence is the CONTRAST between two capabilities on one credential in one minute, which a single request cannot see, so it belongs in a bounded M10 capability probe and is surfaced rather than rush-wired. Baton sealed at docs/operations/batons/WM-PRO-SHIFT-2026-09-18-THE-ARTERY-REACHES-FOUR.md. ATOM 13, 2026-09-18: the private-bar census is now CLASSIFIED, not just counted. Judgement taken on all four residents; NONE is an unmigrated duplicate, so the honest UNMIGRATED count is ZERO and the census total stays pinned at four. ZERO INGRESSES WERE MIGRATED BY THIS CHANGE — it is a measurement correction. DeckMarketChart::Candle = INCOMPLETE_OBSERVATION (volume optional, deck forwards none); CanonicalMarketEvent = NOT_A_BAR (tape event, all four OHLC optional); AnatomyBar = DERIVED_VIEW (absorbing/effortNorm are verdicts no venue sends); AnatomyBarInput = ADAPTER_INPUT (askVol/bidVol split CanonicalBar does not carry). Each verdict is re-derived from the real declaration body every run, so a classification cannot be won by writing a comment. Four mutation receipts, all EXIT 1. The first run caught MY OWN wrong predicate: I asserted the deck Candle carried no volume field; it carries volume? — optionality is the distinguishing fact. ATOM 14, 2026-09-18: retired the LAST silent drop on the finnhub lane, which was in the CHART, not the route. MainChart.fetchFinnhubCandles filtered open > 0 and high > 0 off the response: silent (an invisible gap, not a shorter chart), an amputation (negative crude oil 2020 was a real auction, and checkBarGeometry deliberately does NOT refuse a price for being negative), and asymmetric BY ACCIDENT — fetchFinnhubCandlesDirect requests the IDENTICAL URL, filters nothing, and runs FIRST, so two readers of one endpoint disagreed about which bars exist and neither could see the disagreement. It was cleaning up after the route's own now-retired fabrications; the route refuses across all six fields at ingress and publishes refusedBars. Protection moved upstream, got stricter, started counting. Mutation receipt EXIT 1 on revival. NOT PROVEN live: the finnhub candles lane is 403 on this credential, so this repair is code-proven and live-unobservable.
| M9 — replay must drive the real past | OPEN | `src/components/chart/BarReplayControls.tsx` `src/components/chart/MainChart.tsx` `src/components/chart/ChartsDashboard.tsx` `src/components/chart/barReplayDisclosure.test.tsx` | 2026-09-18 | REPAIR 1 OF 2 (DISCLOSURE) IS CLOSED 2026-09-18; THE ROW STAYS OPEN FOR THE REAL WIRE. The panel no longer narrates a chart it does not drive: while `chartFollowsCursor` is false it renders the reason in words and the way out, and renders NO walking timestamp, NO progress fill and NO position-of-total, because each of those three is a sentence about a chart that is not moving. The flag is a REQUIRED prop with no default, so a future call site cannot inherit the flattering answer by saying nothing. And the disclosure is ratcheted rather than trusted: `barReplayDisclosure.test.tsx` fails if any call site asserts `chartFollowsCursor={true}` while `MainChart.tsx` still merely declares and destructures `replayBars` — the flag may not claim more than the code can back. Three mutation receipts: deleting the disclosure branch fired exactly the three disclosure tests, and flipping the call site to true fired exactly the ratchet with the wire-it-or-revert message. LIVE OBSERVATION 2026-09-18, and the distinction in it is the point: fetching all 23 script bundles served by https://wealthymindsetspro.com/charts and scanning 2,242,709 bytes found the disclosure sentence once and the `chart-follows-cursor` attribute once, so the repair IS DEPLOYED to production. It is NOT proven RENDERED. The panel only mounts while replay is active, and reaching that state means toggling the Founder's live session, which this shift will not do for a screenshot. DEPLOYED is the claim; RENDERED is not, and the row will not borrow the stronger word. WHAT IS STILL OPEN, stated plainly: repair 2, the real wire, which must read FROZEN CanonicalBar ancestry and truth epochs. Disclosing a lie is not repairing it. THE ORIGINAL MEASUREMENT, unchanged, follows. RE-MEASURED 2026-09-18 AND CONFIRMED — worse than the inherited claim, and it is a live lie rather than an inert gap. Exact counts, reproducible with grep: `replayActive` appears TWICE in `MainChart.tsx` and `replayBars` appears TWICE — in both cases once in the props interface and once in the destructure, and zero times anywhere else. The chart is handed a flag it never reads. Worse, `replayBars` is never PASSED at all: `ChartsDashboard.tsx` sends `replayActive` at two call sites and sends `replayBars` at none. Meanwhile the controls are fully alive — a 500/speed ms interval advances `replayIdx`, and `BarReplayControls` renders a BAR REPLAY badge, a position-of-total counter and a `currentTime` read from `chartBars[replayIdx].time`. So the trader presses play, watches the badge light and the timestamp walk backward through the session, and the chart behind it never moves. That is not an unfinished feature, it is a control that reports motion the market never made — the same class of defect as the NO FEED badge that contradicted a loaded chart. TWO SEPARABLE REPAIRS, and they must not be confused: (1) DISCLOSURE — the trader must not be shown a moving replay position over a chart that is not replaying; (2) the real wire, which per the order must read FROZEN CanonicalBar ancestry and truth epochs, never re-derive today's version of old bars, or the past silently becomes a reconstruction that agrees with the present. Repair 2 depends on the M8 adoption half and cannot be honestly rushed. DO NOT FAKE-WIRE THIS BY SLICING TODAY'S BARS — that would turn a visible lie into an invisible one. |
| M10 — provider/broker commissioning | OPEN | `src/lib/broker/certification.ts` | 2026-09-18 | The twelve-stage ladder and its taxonomy exist; what does not exist is a runner that EXECUTES them, so the execution rungs sit UNPROBED rather than measured. Definitions are not commissioning. A market-data-only lane may honestly mark execution stages NOT_APPLICABLE; a broker lane may not call credentials EXECUTABLE, and EXECUTABLE is not RECONCILABLE. |
<!-- GATE-STATUS:END -->

## The FINAL-LAP BREAKER MAP (M1–M10) lives in the table above

The Founder issued a finite repair board on 2026-09-18 — ten breakers, M1
through M10 — and issued it **as a message**. A board that exists only in a
prompt is the same CONVENTION this document was created to replace, and it
drifts by exactly the mechanism recorded at the top of this file. So the ten
rows now sit in the gated table, where each one must name an owner that exists
on disk and a date it was last measured.

**THE STOP-GROWING RULE, WHICH IS PART OF THE ORDER.** There is no M11. A new
breaker requires concrete current repo or runtime evidence of a defect that no
existing row owns. Anything else routes into the breaker it belongs to. An
inspection permitted to grow becomes a second project, and the point of a finite
board is that it can be finished.

**Inherited rows are marked as such, and that is not hedging.** M1 and M5 were
re-measured this shift and both turned out to be PARTLY STALE — the rooms rail
already stays shut on the instrument view, and the route registry already keeps
`frame`, `tier` and phone-bar membership as three separate facts. M2, M3 and M9
carry claims nobody has re-read against current code, and they say so. A row
that admits it was inherited can be checked; a row that quietly asserts a
measurement nobody took is how a shift gets spent re-proving something already
true. The repair order itself says to inspect the actual wiring, and the two
corrections above are what that instruction bought.

## The rule that keeps this honest

Every row carries a MEASURED date. A status with no date cannot go stale — it
can only be quietly wrong, because a reader has no way to tell "true today" from
"true in September and never revisited". The sentinel does not check that a date
is HONEST; nothing here can. It checks that each claim is DATEABLE, which is what
lets a later reader distrust it.

And every row names an OWNER PATH that must exist on disk. A gate pointing at a
file nobody kept is a gate that can never be closed or re-opened — it just sits
there looking like work.
