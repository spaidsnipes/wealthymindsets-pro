# CANON — SHIFT GATE STATUS

**Any agent working the ATHOS WM Pro shift reads the gate list HERE, not from the shift prompt.**

## Why this file exists

The standing shift prompt carries a list of "remaining gates". That list is
retyped from shift to shift, by hand, from memory. On 2026-09-18 two of its
entries were measured and found already closed:

- **Delta Bubbles level ownership** — closed by `src/lib/deltaBubbleLevels.ts`
  plus an adoption sentinel, 18 tests green.
- **Live VP render geometry proof** — closed by `scripts/prove-vp-pixels.mjs`,
  which paints through the shipped geometry owners on a real browser canvas and
  reads the bytes back, and which can be made to fail five different ways.

Nothing was wrong with the code. **The list had drifted from it.** That is the
identical failure `CANON-VIEW-BUILD-ORDER-2026-09-17.md` records for the view
build order, where an operator spent three atoms re-opening shipped work.

A work-list retyped each shift is a CONVENTION. **AN OWNER BEATS A CONVENTION.**
The wrong answer a stale gate list produces is not a broken pixel — it is a
whole shift spent re-proving something already true, and it leaves no trace,
because re-opened work looks exactly like work.

The table below is gated by `src/lib/ops/shiftGateStatus.sentinel.test.ts`.
Prose in this file is commentary. **The table is the claim.**

## What a STATE means here

| STATE | Meaning |
|---|---|
| `CLOSED` | An instrument exists, was run, and passed. The EVIDENCE column names what to run to see it again. |
| `OPEN` | Real remaining work. Nobody is blocked; it has not been done. |
| `BLOCKED` | Cannot proceed for a reason outside this repo. The blocker is NAMED. |

`CLOSED` is a claim about an INSTRUMENT, not about beauty. A gate can be closed
and the surface still ugly; that is a different gate and should be its own row.

<!-- GATE-STATUS:BEGIN -->
| GATE | STATE | OWNER | MEASURED | EVIDENCE |
|---|---|---|---|---|
| Delta Bubbles level ownership | CLOSED | `src/lib/deltaBubbleLevels.ts` `src/lib/deltaBubbleLevels.adoption.sentinel.test.ts` | 2026-09-18 | Run `./node_modules/.bin/vitest run src/lib/deltaBubbleLevels.adoption.sentinel.test.ts` — EXIT 0. The extraction fixed two real defects the sentinel now pins: a bubble printing a bucket CENTRE as the price flow happened at, and a rounded price used as a bucket identity, which silently merged buckets and dropped aggressor volume. |
| Live VP render geometry proof | CLOSED | `scripts/prove-vp-pixels.mjs` `src/lib/vpDrawGeometry.ts` | 2026-09-18 | Run `npm run prove:vp-pixels` — EXIT 0, 5 pixel laws hold against real painted bytes read via `getImageData()`. The instrument is falsifiable: `WM_VP_REVIVE=ROW_HEIGHT\|BAR_CURVE\|SPLIT_ROUND\|FITS_LIE\|AXIS_BLEED` each exit 1 with 1/22/1/2/3 offences respectively. **SCOPE:** this proves the GEOMETRY OWNERS paint honestly. It does NOT prove MainChart's composed scene — colours, labels, POC/VAH/VAL lines over live price — which stays HUMAN_PROOF_REQUIRED and is its own row when someone opens it. |
| Decision Memory sealing has zero production callers | OPEN | `src/lib/traderMemory/decisionMemory.ts` `src/lib/decisionMemory.ts` `src/lib/decisionMemoryReachability.test.ts` | 2026-09-18 | Re-measured, still true, and NOT stale. `grep -rn "sealDecision\b" src/` finds no non-test caller outside the module and its store; `sealDecisionMemory` has none at all. **ARCHITECTURAL — SURFACE IT, DO NOT RUSH-WIRE.** The write path exists and is tested; what is missing is a decision ingress, and inventing one to make a caller appear would mint decisions no trader made. |
| executionConnectivity orphaned | OPEN | `src/lib/authority/executionConnectivity.ts` `src/lib/authority/executionReadinessDisclosure.test.ts` | 2026-09-18 | `grep -rn "from .*executionConnectivity" src/` returns no non-test importer. **NOT A LIVE DEFECT** — `/readiness` discloses the absence honestly rather than claiming connectivity it does not have, and the disclosure test passes. This row exists so nobody re-derives that conclusion; deleting the module is a separate decision from wiring it. |
| Paper execution state machine realism | OPEN | `src/lib/paperTrade.ts` `src/lib/paperTradeOutcome.ts` | 2026-09-18 | Not measured against a realism standard this shift; the state machine has branch tests but no row here yet stating what "realistic" would mean. **The first work on this gate is to write down the standard, not to change the fills** — a realism change with no stated standard cannot be reviewed or reverted. |
| Gate 4 responsive device proof | BLOCKED | `scripts/audit-phone-parity.mjs` | 2026-09-18 | BLOCKED BY: programmatic window resize does not take effect in the available browser channel — `outerWidth` stays pinned, so a 390px claim would be measured against a viewport that never narrowed. The instrument exists and runs; what is missing is a channel that can actually resize. A green run here today would be a false pass, which is worse than the open gate. |
| /journal detail canvas | BLOCKED | `src/app/journal/page.tsx` | 2026-09-18 | BLOCKED BY: zero journal entries exist to open a detail view on. Any proof would have to be built against a fabricated entry, and a canvas verified against invented data proves only that the fabricator agrees with itself. Unblocks the moment a real entry is logged. |
| M1 — live-market destination mall | CLOSED | `src/components/os/WMOperatingSystem.tsx` `src/components/experience/WMExperienceShell.tsx` `src/components/layout/ShellAccessParity.test.tsx` | 2026-09-18 | CLOSED BY RE-HOMING, NOT BY DELETION. The inherited claim was half-stale: the rail already defaulted shut on the instrument view (`WMExperienceShell.tsx:484`) and the mode bar already collapsed there (`:496`). The half that was real was the PHONE — at 390 the same room still carried a pinned five-door strip across the bottom of the market plus a 66px reservation, so /charts was a workspace on the desk and a consumer app with a tab bar on the phone. New `phoneDestinations` prop: on the instrument view the strip is not drawn and the rail itself becomes the phone's navigation, pinned, reached through the SAME masthead toggle the desk uses and carrying all 21 destinations where the strip carried 5 — a gain of sixteen doors, with an explicit 44px Close so an opened sheet is not a trap. Proof: `./node_modules/.bin/vitest run src/components/layout/ShellAccessParity.test.tsx` EXIT 0, 20 passed. Mutation receipts: forcing `phoneDestinations="bar"` went red on "draws NO pinned destination strip over the market"; removing the Close control went red on "gives the opened sheet a way back out". Both restored EXIT 0. LIVE-PROVEN on https://wealthymindsetspro.com/charts after `ba0f413e` deployed — probed in the Founder's own authenticated Chrome, NOT a local build: `phoneNav:false` (the strip element is gone from the document entirely), `sheetRule:true` (the phone sheet CSS ships), `provReservation:false` (the 66px reservation is no longer emitted). Opening the masthead toggle live returned `railOpen:true, doorsInRail:21, railClosePresent:true, railCloseLabel:"Close rooms"` — the sixteen-door gain is real in production, not only in a test. The Founder's rail was then closed again USING the new Close control, which both restored his state and proved the escape works: `railOpen:false, expanded:"false"`. WHAT IS STILL NOT PROVEN, stated plainly: the 390px VISUAL. The strip's absence is a render branch and therefore width-independent, so that half is proven; the pinned-sheet geometry under a real 390 viewport is not, because programmatic resize does not take in this environment — see the Gate 4 BLOCKED row. |
| M2 — Workspace/Tools FULL stage | CLOSED | `src/components/experience/RoomEquipmentLayer.tsx` `src/lib/workspace/useEquipmentJourney.ts` `src/components/experience/RoomEquipmentLayer.test.tsx` | 2026-09-18 | RE-MEASURED AND LARGELY REFUTED, then the one real gap was closed. All three inherited observations are literally true and none is the defect the claim assumed. FULL takes the chart away ONLY at full, deliberately, and it is the single stage carrying RETURN; `useEquipmentJourney` restores the ROOM's exact scrollTop on the way out and its own comments record that reading window.scrollY instead was tried and measured wrong. Fixed `inset:0` is not the bug, it IS the fix — a fixed overlay is what keeps the chart MOUNTED underneath, so symbol, timeframe, MarketObjects and chart state survive by construction rather than by restoration. The URL carries a REFLECTION, not a source of truth, and `full` is explicitly not cold-openable so a shared link cannot drop a stranger into a full-screen reading with no chart behind it. WHAT WAS GENUINELY UNGUARDED: thirty-plus assertions next door check where the grammar is wired and what it refuses, and not one checked that the shell stays OUT of the room's layout flow — so a future simplification of FULL into an in-flow section would reflow the chart out of the room and the whole suite would stay green. Three tests added: every open depth is out of flow, only FULL claims `inset:0`, and the decision identity is published unchanged at every depth. Proof: `./node_modules/.bin/vitest run src/components/experience/RoomEquipmentLayer.test.tsx` EXIT 0, 12 passed. Mutation receipts: turning FULL into `position:relative` went red on the out-of-flow test AND the inset test; deleting the `data-decision-id` attribute went red on the identity test; both restored EXIT 0. |
| M3 — Command Deck route gravity | OPEN | `src/app/command-deck/page.tsx` | 2026-09-18 | Inherited claim, NOT re-measured this shift. Deck-only organs must be INVENTORIED and migrated to chart / inspect / receipt / review / diagnostics before the route is demoted. Deleting first is capability amputation; parity is the precondition for retirement, not a follow-up. |
| M4 — legacy tests protecting legacy architecture | CLOSED | `src/components/layout/ShellAccessParity.test.tsx` | 2026-09-18 | REMODELLED IN THE SAME COMMIT AS M1, which is the whole law. The five-door assertions were not wrong and were NOT deleted — they still guard every room where choosing where to go next IS the job. What was missing is that this file rendered the shell exactly ONCE, with no route, so every assertion described the non-instrument shape and nothing here had an opinion about /charts. That is how the two widths disagreed with no gate able to see it. The route is now an input: `usePathname` is mocked, both shapes are rendered, and a new block asserts the instrument view draws no strip AND still reaches more doors than the strip did. Proof: `./node_modules/.bin/vitest run src/components/layout/ShellAccessParity.test.tsx` EXIT 0, 20 passed (was 14). |
| M5 — route registry overload | OPEN | `src/lib/routing/wmDestinations.ts` | 2026-09-18 | PARTLY REFUTED ON MEASUREMENT. `WmDestination` (`:119-143`) already separates its `frame` field (os / cleared / legacy) from its `tier` field (1 or 2), and phone-bar membership is a THIRD list (`PHONE_SLOT_HREFS`, `:373`) rather than being derived from `frame`. The conflation named in the order is not present as described. What is unproven is whether the three concepts stay independent under M1, which is what this row should be re-measured against. |
| M6 — source poison | OPEN | `docs/operations/CANON-VIEW-BUILD-ORDER-2026-09-17.md` `src/lib/ops/shiftGateStatus.sentinel.test.ts` | 2026-09-18 | Stale comments, tests and route prose that can reteach retired architecture to a fresh worker. This repo already owns the instrument for it: `viewBuildOrder.sentinel.test.ts` requires any ungated build-status word to name the day or commit it was last true for. The generalisation, not yet built, is that rule applied to route and shell prose. |
| M7 — permission-born DECISION_ID persistence | OPEN | `src/lib/traderMemory/decisionMemory.ts` `src/lib/traderMemory/decisionMemoryStore.ts` | 2026-09-18 | A decision minted at permission crossing lives in React state until a later intent write, and the store says in its own words that it cannot survive reload or cross-device. **DO NOT FABRICATE AN INTENT TO PERSIST THE ROW** — that mints a decision no trader made, which is a worse defect than the one being fixed. The seam needed is birth/existence, separate from expression. Shares an owner with the "Decision Memory sealing" row above; close them together or they will disagree. |
| M8 — CanonicalBar adoption + object vocabulary | OPEN | `src/lib/marketData/canonicalBar.ts` `src/lib/marketData/marketObjectKinds.ts` `src/lib/marketData/canonicalBarAdoption.sentinel.test.ts` | 2026-09-18 | VOCABULARY HALF CLOSED `540a2cc5`: the kind is spelled `GAP_FVG`, the wrong total is out of the prose and test title, and a guard now fails if a count is retyped beside the array. Re-run `./node_modules/.bin/vitest run src/lib/marketData/marketObjectKinds.test.ts` — EXIT 0. THE ADOPTION HALF IS STILL FULLY OPEN and is the larger one. MEASURED 2026-09-18, not assumed: the artery has ZERO production consumers — every import of it in this repo is a test importing it to test it — while production declares its own open/high/low/close in 22 other places. Twenty-two shapes is not twenty-two styles, it is twenty-two pasts, because two modules can each hold a different 09:31 for the same symbol and neither is wrong by its own lights. A RATCHET now freezes that census so the sprawl can only SHRINK while the migration runs: `./node_modules/.bin/vitest run src/lib/marketData/canonicalBarAdoption.sentinel.test.ts` — EXIT 0, 4 passed. Two mutation receipts earned: a 23rd OHLC shape appended to `src/lib/timeframes.ts` turned it red on the added branch, a phantom entry in the frozen list turned it red on the removed branch, both restored and re-verified green. THE RATCHET IS NOT THE FIX AND DOES NOT CLAIM TO BE — a green run means only "the sprawl has not grown". This row stays OPEN until a real ingress is migrated and the frozen list is lowered in the same commit. THE FIRST MIGRATION ATTEMPT FOUND A WALL, 2026-09-18, and the wall is the reason adoption is zero rather than neglect being the reason: `CanonicalBar.sessionId` is REQUIRED and NOTHING in this product produces one. The only session known anywhere is `CanonicalMarketEvent.sessionId`, which is OPTIONAL and is populated by exactly one adapter — `webullTicksBrowser.ts` passing through the provider's own `tradingSession`. So every candidate ingress faced two bad doors: invent a session, which launders unknown data into a canonical field and is precisely the failure the deliberately-absent inverse of `toLegacyTuple` exists to prevent; or refuse every non-Webull bar, which amputates the live chart. A THIRD DOOR IS NOW BUILT: `SESSION_UNKNOWN` plus `isSessionKnown`, an assertion of ignorance that travels with the bar. `admitBar` now REFUSES a bar that claims EXECUTABLE fidelity while its session is unknown — a price is a different fact inside RTH than outside it — and it refuses rather than silently downgrading, because a quiet correction hides from the caller that the house disagreed with it. Unknown-session bars at the four non-executable fidelities are still admitted, so the chart keeps drawing. `./node_modules/.bin/vitest run src/lib/marketData/canonicalBar.test.ts` — EXIT 0, 27 passed (was 22). One mutation receipt: disabling the guard turned exactly the refusal test and the no-silent-downgrade test red, both restored. STATED PLAINLY SO NOBODY READS THIS AS PROGRESS IT IS NOT: THE CENSUS DID NOT MOVE. It still holds at twenty-two. This commit removed the blocker that every ingress migration would have hit; it migrated nothing. THE CENSUS HAS NOW MOVED, 2026-09-18: TWENTY-TWO TO TWENTY-ONE, and the frozen list was lowered in the same commit that retired the shape, which is the condition this row set for itself. RETIRED: `hooks/useWebSocket.ts::OHLCVBar`. It was a verbatim six-field copy of `liveBarPolicy.ts::LiveBar` — the very module that produced every value the hook ever stored in it — so this was not a disagreement waiting to happen, it was two names for one shape in a file that already imported the producer. The hot-path ref is now typed `LiveBar`, which is what it actually holds, and the PUBLISHED bar is typed `LegacyOhlcvTuple`, the artery's one sanctioned legacy name, which exists precisely so files that still speak six bare numbers have something to be adapted FROM instead of a reason to redeclare. THE SMALLER CLAIM IS THE TRUE ONE AND IS RECORDED HERE SO A LATER READER CANNOT INFLATE IT: this did NOT give the live path canonical IDENTITY. The published bar still carries no symbolId, no sessionId, no fidelity, no provenance and no truthEpoch. A duplicate past was deleted; identity on the live path is still owed, and that is why this row stays OPEN. FOUR MORE IMPRESSIVE REPAIRS WERE REJECTED AS THEATER OR AS NEW DEFECTS, named here so they are not re-attempted: routing live ticks through `admitBar`, which is a collision resolver rather than an in-place updater and refuses an equal `truthEpoch` at the same `asOf`, so it would have refused every tick after the first; manufacturing a CanonicalBar and immediately discarding it; adding a canonical field no consumer reads; and mixing units across one interface, since `applyTickToLiveBar` buckets in SECONDS while `receivedAt` is milliseconds. A SECOND-WRITER BUG FELL OUT OF THE RETYPE and was fixed in the same change: `MainChart.tsx` was assigning straight into the published bar's high, low and open — mutating an object React had already handed out from the hook's state, a second writer to someone else's state that the hook could not see. Making the published bar readonly surfaced it; three locals now carry the garbage-OHLC repair with identical numbers and one writer. Mutation receipt on the ratchet: removing the census entry while the shape still existed turned it red on the removed branch with the scoreboard message, restored and re-verified. A GATE WAS REPAIRED RATHER THAN ITS LEDGER TRIMMED, and this is the part most worth reading: typing the published bar via `import type` made `screenReach.enforcement.test.ts` report `canonicalBar.ts` as newly REACHED, and its own failure message invited the flattering fix — delete the ledger entry so the count stays honest. That would have recorded the artery as having a screen because one TYPE crossed a boundary. A type-only import is ERASED, not tree-shaken, so the emitted bundle contains no reference to the module at all; the walk now strips whole type-only statements before counting edges, deliberately narrow enough that an INLINE type modifier still counts as reach because such a statement still emits its module for its value binding. The repair paid for itself immediately by exposing a pre-existing overclaim: `src/lib/broker/BrokerAdapter.ts` has seven exports, all of them types or interfaces, and all nine of its importers reach it with `import type`, so it ships no runtime code to any screen and had been mis-credited as reached. It is now on the ledger under a NEW reason, `TYPE_ONLY`, which is the one reason in that vocabulary that is not a debt — a contract emitting nothing cannot be wired to a surface, and filing it as AWAITING_SURFACE would invite someone to finish it by adding a runtime export it has no reason to have. The exclusion is itself guarded by five explicit source-text cases including the inline-modifier case a naive regex gets wrong, because deleting that one replace call would have broken no assertion on the day it was deleted. Gates: `./node_modules/.bin/tsc --noEmit` EXIT 0 and `./node_modules/.bin/vitest run` EXIT 0 at 807 files, 10202 passed, 2 skipped — and the total MOVED by exactly the one test added, which is the check that the new guard is genuinely being collected rather than silently skipped. TWENTY-ONE TO TWENTY, 2026-09-18, AND THIS ONE IS A SMALLER CLAIM THAN THE LAST: `types/index.ts::OHLCVBar` is retired by DELETING THE WHOLE FILE IT LIVED IN, because that barrel had ZERO importers. Not few — none. Nothing under src or anywhere else in the repo referenced `@/types` or `src/types/index`, there is no `export *` anywhere that could have re-exported it, and the only path alias is `@/*` to `./src/*`, so the resolution had nowhere else to come from. The sibling `@/types/chart` IS imported and is untouched. Two of the barrel's twelve exports, `Post` and `SignalStrength`, had already been RE-DECLARED inside the files that wanted them — `app/lounge/page.tsx` and `SmartMoneyPanel.tsx` — which is the signature of a barrel that was abandoned rather than one still being written toward. THE WHOLE FILE WENT AND NOT JUST THE COUNTED LINE, deliberately: excising one interface and leaving eleven other unreferenced ones behind would have moved this number while leaving the actual poison in place, since a future reader greps OHLCVBar, finds `@/types`, and learns that importing a bar from there is a thing this product does. That would have been a change made for the scoreboard rather than for the code. STATED SO NOBODY READS IT AS MORE THAN IT IS: this migrated no ingress. It removed a declaration no ingress was using — dead source the census had been counting as if it were live — so of the two steps from twenty-two to twenty, only the FIRST touched running code. NO CAPABILITY WAS AMPUTATED because there was no capability: an interface nothing imports has no consumer to migrate, parity with nothing is what tsc and the full suite staying green demonstrate, and git history keeps the text. One stale sentence in the screen-reach ledger that named `types/index.ts` as a live duplicate was corrected in the same commit, which is the M6 rule applied to a file this very commit deleted. Mutation receipt: re-adding the census entry while the file stayed deleted turned exactly one test red on the removed branch with the scoreboard message, restored and re-verified EXIT 0. Gates after deletion: `tsc --noEmit` EXIT 0 and `vitest run` EXIT 0 at 807 files, 10202 passed, 2 skipped — the total correctly did NOT move, because this change adds and removes no test. LIVE OBSERVATION 2026-09-18, AND WHAT IT CANNOT REACH IS NAMED FIRST. The `MainChart` de-mutation is the only part of this work that changes running behaviour, and it is the part production cannot currently prove, for two independent reasons. One: it introduces no new string. Renaming three reads into three locals compiles to the same minified arithmetic, so the bundle-scan channel that proved M9 DEPLOYED has nothing to look for here and would be dishonest if forced to produce something. Two: the fold it guards only runs on a live tick, and the observation was taken at 21:13 Central on a Friday with the equity session closed, so no tick was available to fold. WHAT WAS ACTUALLY OBSERVED, and it is a weaker claim than either of those: a NEW BUILD IS BEING SERVED — refetching /charts returns a chunk set that differs from the one the Founder's already-open tab has loaded, one chunk added and two gone, which is what a rebuild of changed modules looks like — and /charts, /readiness and /journal all return HTTP 200 with full shells, so nothing in this work broke the build or the route. THE FOUNDER'S SESSION WAS NOT TOGGLED AND HIS TAB WAS NOT RELOADED to manufacture a better-looking receipt. THE HONEST STATE IS THEREFORE: the de-mutation is REASONED AND UNIT-COVERED, the route is SERVING, and the live-bar fold is UNOBSERVED — it needs a session with real ticks, and until one is available this row will not say otherwise. |
| M9 — replay must drive the real past | OPEN | `src/components/chart/BarReplayControls.tsx` `src/components/chart/MainChart.tsx` `src/components/chart/ChartsDashboard.tsx` `src/components/chart/barReplayDisclosure.test.tsx` | 2026-09-18 | REPAIR 1 OF 2 (DISCLOSURE) IS CLOSED 2026-09-18; THE ROW STAYS OPEN FOR THE REAL WIRE. The panel no longer narrates a chart it does not drive: while `chartFollowsCursor` is false it renders the reason in words and the way out, and renders NO walking timestamp, NO progress fill and NO position-of-total, because each of those three is a sentence about a chart that is not moving. The flag is a REQUIRED prop with no default, so a future call site cannot inherit the flattering answer by saying nothing. And the disclosure is ratcheted rather than trusted: `barReplayDisclosure.test.tsx` fails if any call site asserts `chartFollowsCursor={true}` while `MainChart.tsx` still merely declares and destructures `replayBars` — the flag may not claim more than the code can back. Three mutation receipts: deleting the disclosure branch fired exactly the three disclosure tests, and flipping the call site to true fired exactly the ratchet with the wire-it-or-revert message. LIVE OBSERVATION 2026-09-18, and the distinction in it is the point: fetching all 23 script bundles served by https://wealthymindsetspro.com/charts and scanning 2,242,709 bytes found the disclosure sentence once and the `chart-follows-cursor` attribute once, so the repair IS DEPLOYED to production. It is NOT proven RENDERED. The panel only mounts while replay is active, and reaching that state means toggling the Founder's live session, which this shift will not do for a screenshot. DEPLOYED is the claim; RENDERED is not, and the row will not borrow the stronger word. WHAT IS STILL OPEN, stated plainly: repair 2, the real wire, which must read FROZEN CanonicalBar ancestry and truth epochs. Disclosing a lie is not repairing it. THE ORIGINAL MEASUREMENT, unchanged, follows. RE-MEASURED 2026-09-18 AND CONFIRMED — worse than the inherited claim, and it is a live lie rather than an inert gap. Exact counts, reproducible with grep: `replayActive` appears TWICE in `MainChart.tsx` and `replayBars` appears TWICE — in both cases once in the props interface and once in the destructure, and zero times anywhere else. The chart is handed a flag it never reads. Worse, `replayBars` is never PASSED at all: `ChartsDashboard.tsx` sends `replayActive` at two call sites and sends `replayBars` at none. Meanwhile the controls are fully alive — a 500/speed ms interval advances `replayIdx`, and `BarReplayControls` renders a BAR REPLAY badge, a position-of-total counter and a `currentTime` read from `chartBars[replayIdx].time`. So the trader presses play, watches the badge light and the timestamp walk backward through the session, and the chart behind it never moves. That is not an unfinished feature, it is a control that reports motion the market never made — the same class of defect as the NO FEED badge that contradicted a loaded chart. TWO SEPARABLE REPAIRS, and they must not be confused: (1) DISCLOSURE — the trader must not be shown a moving replay position over a chart that is not replaying; (2) the real wire, which per the order must read FROZEN CanonicalBar ancestry and truth epochs, never re-derive today's version of old bars, or the past silently becomes a reconstruction that agrees with the present. Repair 2 depends on the M8 adoption half and cannot be honestly rushed. DO NOT FAKE-WIRE THIS BY SLICING TODAY'S BARS — that would turn a visible lie into an invisible one. |
| M10 — provider/broker commissioning | OPEN | `src/lib/broker/certification.ts` | 2026-09-18 | The twelve-stage ladder and its taxonomy exist; what does not exist is a runner that EXECUTES them, so the execution rungs sit UNPROBED rather than measured. Definitions are not commissioning. A market-data-only lane may honestly mark execution stages NOT_APPLICABLE; a broker lane may not call credentials EXECUTABLE, and EXECUTABLE is not RECONCILABLE. |
<!-- GATE-STATUS:END -->

## The FINAL-LAP BREAKER MAP (M1–M10) lives in the table above

The Founder issued a finite repair board on 2026-09-18 — ten breakers, M1
through M10 — and issued it **as a message**. A board that exists only in a
prompt is the same CONVENTION this document was created to replace, and it
drifts by exactly the mechanism recorded at the top of this file. So the ten
rows now sit in the gated table, where each one must name an owner that exists
on disk and a date it was last measured.

**THE STOP-GROWING RULE, WHICH IS PART OF THE ORDER.** There is no M11. A new
breaker requires concrete current repo or runtime evidence of a defect that no
existing row owns. Anything else routes into the breaker it belongs to. An
inspection permitted to grow becomes a second project, and the point of a finite
board is that it can be finished.

**Inherited rows are marked as such, and that is not hedging.** M1 and M5 were
re-measured this shift and both turned out to be PARTLY STALE — the rooms rail
already stays shut on the instrument view, and the route registry already keeps
`frame`, `tier` and phone-bar membership as three separate facts. M2, M3 and M9
carry claims nobody has re-read against current code, and they say so. A row
that admits it was inherited can be checked; a row that quietly asserts a
measurement nobody took is how a shift gets spent re-proving something already
true. The repair order itself says to inspect the actual wiring, and the two
corrections above are what that instruction bought.

## The rule that keeps this honest

Every row carries a MEASURED date. A status with no date cannot go stale — it
can only be quietly wrong, because a reader has no way to tell "true today" from
"true in September and never revisited". The sentinel does not check that a date
is HONEST; nothing here can. It checks that each claim is DATEABLE, which is what
lets a later reader distrust it.

And every row names an OWNER PATH that must exist on disk. A gate pointing at a
file nobody kept is a gate that can never be closed or re-opened — it just sits
there looking like work.
