# WM Pro 10-Point Deep Audit — 2026-08-08

**Owner:** one-thread (under Founder 2026-08-08 supersede directive)
**Method:** read-only sub-agent sweep (Explore agent `a9f096a41745f0c08`) run against HEAD `ed4e73a` after the WM-SEC-P0-01/03 remediation landed. Every claim carries a `file:line` citation.

## Critical (P0 SEC-HAZARD — new tickets filed)

### CRITICAL-A: `.env.local` was committed to git history (WM-SEC-P0-04 + WM-SEC-P0-05)

Added in commit `39c8758`, deleted in `3dd6050`. History still contains cleartext for:

- `NEXT_PUBLIC_POLYGON_KEY=cnmi1opnRq5LcPCjppqcH_YAFuQDBpki` — **Polygon key, live** → WM-SEC-P0-05.
- `NEXT_PUBLIC_FINNHUB_KEY=d8efu9hr01qth3ch5f2…` — **already rotated 2026-08-08** per this session; git history remains, but the credential no longer authenticates.
- `ALPACA_KEY=AK4YOXHUA6K67UNNKCHP3OZSJG` + `ALPACA_SECRET=…` with `ALPACA_LIVE=1` — **LIVE Alpaca trading keys** → WM-SEC-P0-04. Highest severity: authorises real orders on the Founder's brokerage account.

Both credentials must be rotated at the provider; the file's presence in history is permanent unless the repo is history-rewritten (BFG/`git filter-repo`).

### CRITICAL-B: `NEXT_PUBLIC_POLYGON_KEY` shipped to browser bundle (WM-SEC-P0-05)

Same class as the Finnhub bug closed today. Live client-side reads:

- `src/components/layout/TickerTape.tsx:10`
- `src/components/chart/MainChart.tsx:178`
- `src/app/api/symbol-search/route.ts:11` (server route reading the public-prefixed name)
- `src/lib/api/coingecko.ts:7` (`NEXT_PUBLIC_COINGECKO_API`; orphan module — deletable)

### CRITICAL-C: Unauthenticated privileged / mutating endpoints (WM-SEC-P0-06)

Every one accepts requests without a session and does one of: write to shared storage, mint privileged tokens, execute real broker orders, spend paid provider quota, send email.

- `src/app/api/upload-track/route.ts:12` — multipart file upload; uses `SUPABASE_SERVICE_ROLE_KEY` to write to public `radio` bucket (also created public at `:27`).
- `src/app/api/alpaca/trade/route.ts:14` — POST executes real orders; accepts key/secret from request body (`:17-30`).
- `src/app/api/alpaca-trading/route.ts:24-33` — supports `ALPACA_LIVE=1` (line 30); POST places live orders with no session gate.
- `src/app/api/tradovate/route.ts:12` — arbitrary `endpoint` + `token` proxy (SSRF-adjacent; path uses `/${endpoint}` at `:20`).
- `src/app/api/livekit/route.ts:5` — mints signed LiveKit `AccessToken` (`:22`) with `canPublish` when `role=host`, no auth.
- `src/app/api/livekit/approve/route.ts:5` — grants publish rights server-side, no auth.
- `src/app/api/emails/welcome/route.ts:4` — unauthenticated Resend send endpoint; email-spam vector.
- `src/app/api/spaidbot/route.ts` — no auth on `GEMINI_API_KEY` use (`:8`); open quota abuse.
- `src/app/api/passport/handoff/route.ts:9` — validates a hashed code but no rate limit / origin check; sets auth cookie for any consumer of a leaked code.
- `src/app/api/broker/{alpaca,coinbase,oanda,kraken,binance}/route.ts` — credential-echo proxies with no session boundary.
- `src/app/api/audio/route.ts:26,52` — no auth; POST/DELETE mutate in-memory track store (low blast-radius).

### CRITICAL-D: Supabase RLS is `USING (true)` on lounge tables (WM-SEC-P0-02 — pre-existing, evidence confirmed)

`supabase-schema.sql:58-79` — `lounge_posts`, `lounge_likes`, `lounge_comments`, `lounge_follows` have RLS enabled but every policy is `USING (true) / WITH CHECK (true)`. Any holder of the anon key can delete any post/like/comment/follow. Comment at `:56` reads "MVP — add RLS later."

## Third-party CVEs (npm audit)

Four **HIGH** severity, no criticals. `next` alone accounts for 8 advisories (SSRF, cache confusion, unauth Server-Function disclosure, DoS via Server Actions, image-opt SVG DoS). Also `postcss ≤8.5.22`, `sharp <0.35.0` (breaking bump), `nanoid <3.3.17`. Fixable via `npm audit fix`; sharp requires `--force`.

## Middleware coverage

`src/middleware.ts:15-27` does exactly one thing: 308-redirect non-canonical `*.vercel.app` hosts to `canonicalHost`. It enforces no authentication. All 20+ authenticated-looking pages (`/charts`, `/journal`, `/paper`, `/ai-bot`, `/scanner`, `/heatmaps`, `/backtesting`, `/copy-trading`, `/creator`, `/radio`, `/shop`, `/lounge`, `/morning-prep`, `/partnerships`, `/tv`, `/education`, `/vailbuild`, `/veddbuild`, …) render client-side and rely entirely on downstream API rejection — which per CRITICAL-C is inconsistent. Only `src/app/profile/page.tsx` self-guards. Consider `middleware.ts` gating the authenticated set.

## Provider-name UI leakage (extended inventory)

Adds to prior reconciliation Section 6. Consolidated file:line list:

- `src/components/broker/AlpacaTradingPanel.tsx:278,388,705` — heading + status + attribution.
- `src/components/layout/MainLayout.tsx:683` — "Finnhub + Polygon.io" badge.
- `src/components/layout/SpaidBotButton.tsx:477-478,496` — three Alpaca mentions.
- `src/components/chart/ChartToolbar.tsx:724` — "Searching Finnhub global database…".
- `src/app/ai-bot/page.tsx:106` — "Alpaca relay is connected".
- `src/app/backtesting/page.tsx:262,312` — Yahoo range note + "real Yahoo OHLCV bars".
- `src/app/news/page.tsx:201,203,208,210,354` — Finnhub loading strings + Yahoo Finance label.

Broker Directory (`src/components/broker/BrokerConnectPanel.tsx:21-185`) legitimately names brokers by identity — acceptable. `src/components/ui/SymbolSearch.tsx:61,97` names assets (Coinbase Global, Polygon / USD), not providers — acceptable.

## Dead code / orphans

Zero-importer modules (`grep @/lib/…`):

- `src/lib/api/coingecko.ts` — confirmed zero importers (prior finding still true, still not deleted; contains `NEXT_PUBLIC_COINGECKO_API`).
- `src/lib/markov.ts` — zero importers outside `.test.ts`. Matches WM-STATE-P0-01 queue note: engine is shipped but "UI wiring intentionally deferred."
- `src/lib/vpEngine.ts` — zero importers outside `.test.ts` — matches WM-DEBT-P2-03 queue row ("VP v2 engine built and tested but un-wired behind `NEXT_PUBLIC_VP_ENGINE=v2`").

No `*.old.ts`, `*.new.ts`, `*.bak`, `*.backup`, `_deprecated_*` files under `src/`.

Two pages exist but weren't in the earlier map: `src/app/vailbuild/page.tsx`, `src/app/veddbuild/page.tsx` — routable + public.

## Console noise

<20 total across `src/app`, `src/components`, `src/hooks`. Loudest: `src/components/chart/LeftSidebar.tsx` (6), `src/components/chart/MainChart.tsx` (2). Nothing logs raw tokens/JWTs/cookies/passwords. `src/app/api/auth/{signup,login}/route.ts` log error objects which may include emails — server-side (Vercel logs), acceptable but noted.

## Bundle heavy imports

Clean. No `lodash` / `moment` / `chart.js` / `d3` / `three` / `@tensorflow` / `pdfkit` / `puppeteer` / `sharp` / `canvas` in `src/app` or `src/components` or `src/hooks`.

## What one-thread will do next

Immediate P0 filings from this audit (see updated `ACTIVE_TASK_QUEUE.md`):

- **WM-SEC-P0-04** — Alpaca LIVE key rotation (Founder-gated at alpaca.markets).
- **WM-SEC-P0-05** — Polygon key rotation + strip `NEXT_PUBLIC_POLYGON_KEY` from all 3 reads (same-class fix as WM-SEC-P0-03).
- **WM-SEC-P0-06** — Auth guards for the 10 privileged/mutating endpoints listed under CRITICAL-C.
- **WM-SEC-P0-07** — Rate-limit + origin check for `passport/handoff` and `emails/welcome`.

WM-SEC-P0-02 (lounge-table RLS) evidence confirmed and re-linked to CRITICAL-D. `npm audit fix` staged behind a separate small ticket to avoid entangling security work with framework upgrade.
